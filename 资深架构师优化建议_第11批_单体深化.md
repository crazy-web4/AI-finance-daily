# AI 财经日报 · 资深架构师优化建议（第 11 批 · 自动化与情报深化）

> 评审日期: 2026-09-05 ｜ 评审基线: dev @ 第 10 批（产品化 12 条已落地、243 测试，工作区含第 10 批未提交改动）
> 评审范围: 全部源码 + config + 模板 + 历史产物 + 历次评审文档
> 性质: 第 10 批解决"用起来舒服"，本批解决三件事 —— **① 自动跑、追得回；② 知识能按公司/趋势查；③ 人与系统能协作改报告**。

---

## 〇、为什么还需要第 11 批（现状盘点）

第 10 批已交付：本地 Web 看板、`today/latest/doctor` 快捷命令、多格式导出（md/公众号/邮件/Notion）、卡片图、RSS/Atom、SQLite 情报库（index/query/stats）、趋势（trend）、周报（weekly）、异常告警（watch）、订阅个性化（subscribe）、优雅降级（fallback/ratelimit）。

代码实测后确认的**剩余真实空白**（每一条都已在代码中核实存在与否）：

| 现状事实 | 证据 | 缺口 |
|---------|------|------|
| 所有命令都要 `python main.py <cmd>`，文档里写的 `ai-daily <subcmd>` 并不存在 | `which ai-daily` 无结果；无 bin/、无 Makefile | 无统一命令入口 |
| 每天出报仍靠手工跑或自己配 cron/launchd；代码里无任何调度 | 全库 grep 无 launchd/crontab/schedule | 无内置定时调度 |
| 某天漏跑/失败后无自动补报；成功也无摘要推送（只有失败告警 send_alert） | run_daily.py 仅失败告警；无成功通知路径 | 无补跑、无日报摘要推送 |
| 情报库只有日期维度；没有"查某家公司"的档案/时间线入口 | `query.py` 无 company 检索；report_store 按日期 | 无公司维度查询 |
| Web 看板是标准库 http.server 纯文本页，无任何图表 | server.py 无 chart/svg/canvas | 无可视化图表 |
| 采集只依赖搜索引擎（529 条 query），无稳定的一手源（官方博客/arXiv） | 全库无 RSS/feedparser 摄入逻辑 | 无精选 RSS 源 |
| 用户会手工改报告（历史产物出现 `_v2.._v11` 版本号泛滥、人工重渲染痕迹） | data/reports 目录、评审记录 T18/E3 | 无结构化的人工编排/覆盖机制 |
| 跟踪公司名单写死在 `config/search_strategy.yaml`，加公司要手工改 yaml + 手工重建查询 | yaml companies 段静态 | 无公司库 CLI 管理 |
| data/raw 32MB + data/reports 124MB 只增不减，版本残留文件无人清理 | du 实测 | 无数据生命周期治理 |
| 每天只产一份全量日报，没有"3 分钟速览"变体 | run_daily 单一路径 | 无简报版 |

> ⚠️ **前置风险（必须先处理，否则本批一切后续不稳）**：第 10 批改动**尚未提交**（12 个文件、+703/-112 行），且 dev 分支领先 origin/dev 1 个提交。交给 codex 前先提交固化基线；同时固定单一 Python 解释器 + `requirements.txt`/venv 入库（当前 3.12/3.14 双解释器、playwright 只在 3.14 可用，见第 9 批 E1）。

---

## 一、本批四大主题总览

| 主题 | 用户价值一句话 | 任务 |
|------|--------------|------|
| **A · 自动化与一键化** | 不再记长命令、不再手工配定时，漏报自动追回 | T-A1 / T-A2 / T-A3 |
| **B · 情报层深化** | 从"看日报"升级为"查公司、看趋势、看一手源" | T-B1 / T-B2 / T-B3 |
| **C · 人机协作与质检** | 不用再手工改 PDF，标注一次、每次生效 | T-C1 / T-C2 |
| **D · 内容形态与生命周期** | 多一份速览、公司库可管、磁盘不膨胀 | T-D1 / T-D2 / T-D3 |

优先级判定：P0 = 直接决定"每天自动稳定出报+知识可查"的核心体验；P1 = 显著提升内容质量或工作流；P2 = 锦上添花/卫生。全部在单体边界内，零强制第三方重依赖（标准库优先，可沿用已允许的 Pillow/httpx）。

---

## 二、前置项（让 codex 开工前先固化基线）

### P0-A · 提交第 10 批未提交改动并冻结基线
- **现状**: 第 10 批全部功能（web/export/cards/feed/index/trend/weekly/watch/subscribe 等）代码在工作区，未 commit。
- **落地**: 先 `git add -A && git commit`（信息 `feat: 第10批 产品化12条落地`），记录基线 commit hash。
- **验收**: `git status` 干净；`python3.12 -m pytest -q` 全绿（243+）。

### P0-B · 固定解释器与依赖
- **现状**: 3.12 / 3.14 双解释器，playwright 只在其中一个可用；第 9 批 E1 已踩坑。
- **落地**: 新增 `requirements.txt`（锁版本）+ 建议 `python3 -m venv .venv`；run_daily.py 启动时预检 playwright 可用性并提前告警（而非跑完 40 分钟才崩）。
- **验收**: `doctor` 增加"解释器/venv/依赖一致"检查项；用唯一解释器跑通 `--test` 全链路含 PDF。

### P0-C · 每批落地后强制双验证
- **落地**: 沿用第 9 批发布检查单 —— 每批 codex 提交后：`pytest` 全绿 + `run_daily.py --test` 真实链路跑通 + `使用实操文档.md` 同步。写入 AGENTS.md。

---

## 三、主题 A · 自动化与一键化

### T-A1 · 统一命令入口 `ai-daily`（P0，~0.5-1 天）

**目标**: 一行 `ai-daily <子命令>` 取代 `python main.py <子命令>`，安装进 PATH，带 tab 补全。

**现状**: 所有命令要敲 `python main.py today` / `python run_daily.py --full`，路径长、易错、文档与命令名不一致（文档已写 `ai-daily` 但不存在）。

**落地后**:
```bash
ai-daily today          # 打开今天日报
ai-daily web            # 起看板
ai-daily schedule enable --at 07:30
ai-daily company OpenAI # 查公司档案
ai-daily <TAB><TAB>     # 子命令补全
```

**关键模块/文件**:
- 新增 `bin/ai-daily`（Python shebang 包装，转发到 main.py / run_daily.py）
- 新增 `scripts/install_ai_daily.sh`：软链到 `/usr/local/bin` 或 `~/.local/bin`，并注册 bash/zsh 补全（`complete -W "$(ai-daily --list)" ai-daily` 或生成 `_ai_daily` 补全脚本）
- `main.py` 增加隐藏子命令 `--list`（输出全部子命令供补全）

**验收标准**:
1. `./bin/ai-daily --help` 与 `python main.py --help` 等价
2. 安装脚本跑一次后 `which ai-daily` 生效，`ai-daily today/latest/web/doctor/export` 全部可用
3. `ai-daily run --full` 等价于 `python run_daily.py --full`（转发）
4. tab 补全在 bash/zsh 各测一次
5. 测试: `tests/test_cli_shortcuts.py` 扩展（wrapper 转发路径、--list 输出）

### T-A2 · 内置定时调度 `ai-daily schedule`（P0，~2 天）

**目标**: 配置一次，每天固定时间自动出报；不需要用户懂 launchd/crontab。

**现状**: "每天自动跑"只能靠用户手工配 cron/launchd，代码里无任何调度能力，`doctor` 也只会提示"检查 cron / launchd"。

**落地后**:
```bash
ai-daily schedule enable --at 07:30 --mode full   # 生成并安装 launchd plist / crontab
ai-daily schedule status                           # 显示下次执行时间、上次执行结果
ai-daily schedule disable                          # 卸载
ai-daily schedule test                             # 立即试跑一次（不装）
```

**关键模块/文件**:
- 新增 `app/scheduler/`（`plist.py` macOS launchd 模板 + `crontab.py` 通用 fallback，~250 行）
- 配置存 `config/schedule.yaml`（`{enabled, at, mode, args, log}`）
- `status` 读取 launchctl / `at` 状态 + 最近 runlog 结果；失败时给出提示
- 被调度命令统一走 `ai-daily run --full`（转发 run_daily）

**验收标准**:
1. macOS 上 `schedule enable --at 07:30` 生成合法 `com.aidaily.run.plist`，`launchctl list | grep aidaily` 可见
2. `schedule disable` 后 launchd 不再有该任务
3. Linux/无 launchd 环境回退 crontab 行（`crontab -l` 校验语法）
4. `schedule status` 在无任务/已安装/上次失败三种状态输出正确
5. 测试: `tests/test_scheduler.py`（plist 生成、crontab 行生成、状态解析，不真装系统任务）

### T-A3 · 漏报补跑 + 成功摘要推送 `ai-daily catchup`（P1，~2 天）

**目标**: 某天没跑/跑挂了能一键补上；每天出报成功后有今日头条摘要推送到手机/邮箱。

**现状**: 只有失败 webhook 告警；错过一天就永远缺一天；成功跑完没有任何推送。

**落地后**:
```bash
ai-daily catchup --days 3          # 检查近3天是否有缺口（无 daily_*.json 或 runlog 失败），逐天补报
ai-daily catchup --force --date 2026-09-03   # 强制重出某天
ai-daily notify-on-success --to feishu|email|local   # 出报成功后推送：今日头条+栏目条数+PDF路径
```

**关键模块/文件**:
- `app/pipeline/catchup.py`（缺口检测：ReportStore.dates() + runlog 状态；逐日调用 run_daily 核心函数）
- `app/utils/digest.py`（生成今日摘要文本：头条 Top3 标题 + 每栏目条数 + 关键数字 + PDF 路径）
- 推送通道复用 `send_alert`（webhook）+ 新增 SMTP 邮件（标准库 smtplib）+ macOS `notify_local`（已存在于 anomaly.py，抽出复用）
- run_daily.py 成功路径末尾调用 digest 推送（默认关闭，`config/schedule.yaml` 或 `config/app_config.yaml` 开关）

**验收标准**:
1. 构造"缺 2026-09-04、09-03 失败"的假目录 → `catchup --days 3 --dry-run` 正确列出 2 个缺口
2. `catchup` 真跑补齐后 ReportStore 能看到对应日期产物
3. 成功推送内容含头条标题与 PDF 绝对路径；webhook 未配置时优雅跳过
4. 邮件发送用本地 SMTP mock 验证（不真发）
5. 测试: `tests/test_catchup.py` + `tests/test_digest.py`

---

## 四、主题 B · 情报层深化

### T-B1 · 公司档案与时间线 `ai-daily company <名称>`（P0，~2-3 天）

**目标**: `ai-daily company OpenAI` 一屏看到 OpenAI 的完整动态史：融资、模型、政策、研究时间线 + 活跃度曲线 + 相关条目清单。

**现状**: 情报库已有 `entities`（公司×日期×栏目）与 `items`（全文 FTS）两张表，但只有日期维度查询；想看某公司只能 `query "OpenAI"` 捞一堆条目再人肉按时间排。

**落地后**:
```bash
ai-daily company OpenAI                     # 终端档案页
ai-daily company OpenAI --days 30 --format markdown   # 导出
ai-daily company --list --top 20            # 活跃公司 Top（近30天）
```
输出（终端/看板共用同一数据源）：
```
OpenAI · 近30天 · 活跃度 ▇▇▇▇▅▃▂（12天有动态，共28条）
  ─ 时间线 ─
  09-05 模型   GPT-5 编码版发布 (91分)
  09-04 融资   完成 65 亿美元融资 (87分)
  ...
  ─ 栏目分布 ─ 模型5 / 融资4 / 政策2 / 研究3 / 产业8
```

**关键模块/文件**:
- 新增 `app/storage/company.py`（~300 行）：基于 entities+items 聚合 —— 时间线（按 date×importance 排序）、栏目分布、活跃度时间序列（count by day）、首末动态
- `main.py` 注册 `company` 子命令
- Web 看板新增 `/company/<name>` 页面 + 首页公司热榜入口

**验收标准**:
1. 索引 5 期以上后 `company "OpenAI"` 正确输出时间线（按日期降序、含分数与栏目）
2. `--list --top 20` 与 entities 聚合 SQL 结果一致
3. 无该公司数据时友好提示"暂无记录，可能未覆盖该公司"（退出码 0）
4. 看板 `/company/OpenAI` 200 且渲染时间线
5. 测试: `tests/test_company.py`（聚合正确性、空态、排序、SQL 复用）

### T-B2 · 看板 SVG 图表（P1，~2 天）

**目标**: 打开看板首页即见 4 张图：近 7/30 天话题热度、公司活跃 Top、运行健康趋势（成功率/耗时）、缓存命中率。

**现状**: 看板是纯文本页（健康页只给数字行），趋势要靠 CLI `trend` 猜。

**落地后**: `http://127.0.0.1:8910` 首页增加内联 SVG 图表（无 JS 库、无外网依赖，标准库直接生成 `<svg>` 字符串）：
- 话题热度折线（近 30 天每日条目数 × 栏目）
- 公司活跃条形图（Top 10）
- 运行健康柱状（近 14 次 run：成功绿/失败红 + 耗时）
- 缓存命中率环图（近 7 天 extract/analysis 命中率）

**关键模块/文件**:
- 新增 `app/web/charts.py`（~250 行）：纯函数 `bar_svg(data) / line_svg(series) / donut_svg(v)`，返回内联 SVG 字符串
- `app/web/server.py` 首页/健康页接入；图表数据来自 `report_store.all_runlogs()` + `storage.query.stats()` + `trend.py`

**验收标准**:
1. 首页渲染 4 张图且无外部资源请求（`curl` 页面仅一个 HTML 响应）
2. 数据为空时每张图显示空态文案而非空白/报错
3. SVG 可被浏览器直接渲染、宽高自适应、文字不重叠
4. 测试: `tests/test_charts.py`（SVG 含 `<svg>`、元素计数、空数据、数值转义防注入）

### T-B3 · 精选 RSS 源摄入 `ai-daily sources`（P1，~2 天）

**目标**: 在搜索引擎之外，稳定摄入一手源（OpenAI/DeepMind/Meta 官方博客、arXiv cs.AI 新论文、权威媒体频道），保证每天有高质量"确定可达"的输入。

**现状**: 采集 100% 依赖 AnySearch/Tavily 搜索，命中质量波动、常青页污染（第四轮时效防线已见端倪）；没有任何一手订阅源。

**落地后**:
```bash
ai-daily sources list                # 查看已订阅源
ai-daily sources add <url>           # 添加 RSS/Atom 源
ai-daily sources fetch               # 手动拉取一次并入库到 data/raw
```
- 采集管线 Step 1 增加"RSS 通道"：拉取订阅源 → 解析条目（标准库 `xml.etree` 手写 RSS/Atom 解析，不引 feedparser）→ 归一化为 `RawNewsArticle` → 与搜索文章合并进同一漏斗（时效/去重/聚类一致）
- 内置默认源清单（可编辑 `config/sources.yaml`）：OpenAI Blog、Google DeepMind Blog、arXiv cs.AI（按日期抓新）、MIT Tech Review 等 5-8 个

**关键模块/文件**:
- 新增 `app/search/rss.py`（~250 行：fetch+parse+normalize，带 5xx 重试与超时）
- 新增 `config/sources.yaml`（源清单 + 每源 max_items）
- `app/pipeline/collector.py` 增加 `collect_sources()` 并入主链路（失败不影响搜索通道）

**验收标准**:
1. 用本地测试 XML（RSS 2.0 + Atom 各一份）解析出正确 title/link/pubDate/description
2. `sources fetch` 拉取后条目以 `RawNewsArticle` 进入 raw 存档，source_domain 正确
3. 源不可达（超时/404）时记 flag 不阻断主链路
4. 与搜索文章合并后时效过滤、去重、聚类逻辑一致（复用同一入口）
5. 测试: `tests/test_rss.py`（解析 golden sample、异常源、去重）

---

## 五、主题 C · 人机协作与质检

### T-C1 · 人工编排覆盖 `ai-daily edit`（P1，~3 天）

**目标**: 每天出报后，用户可对单条做"保留/剔除/置顶/降级/改标题/改详情"，标注一次、每天自动生效；不再手工改 PDF。

**现状**: 历史产物出现 `_v2.._v11` 版本号泛滥 —— 用户现在靠"改 JSON 再重渲染"或"重新跑"完成人工修正，低效且不可复用。

**落地后**:
```bash
ai-daily edit --date 2026-09-05              # 交互式列出条目，选操作
ai-daily edit --date 2026-09-05 --drop "GPT-5 编码版"       # 非交互：按标题/ID 剔除
ai-daily edit --date 2026-09-05 --promote "…" --to headline # 置顶
ai-daily edit --fix-title "旧标题" "新标题"
ai-daily edit list                            # 查看当日已生效的编排
```
- 覆盖规则落盘 `data/curation/{date}.yaml`：`{event_id: {action: keep|drop|promote|demote, title, details, reason}}`
- `run_daily.py` finalize 后 / 重渲染入口应用 curation：drop 剔除、promote 进头条、title/details 覆盖（覆盖后的版本回写分析缓存，避免重跑时被覆盖）
- 提供"按标题模糊匹配 event_id"（复用标题 bigram 相似度）降低使用门槛

**关键模块/文件**:
- 新增 `app/curation/store.py`（YAML 读写）+ `app/curation/apply.py`（应用到 DailyReport）
- `app/agents/pipeline.py` finalize 后挂接；`run_daily.py` 出报后自动加载当日 curation（有则应用并重写 JSON/HTML）
- `main.py` 注册 `edit` 子命令

**验收标准**:
1. `--drop` 后重渲染的 PDF/JSON 不含该条；`--promote` 后该条进入头条栏目
2. `--fix-title` 修改出现在 JSON/HTML/导出件三处；重跑分析时覆盖不回退
3. 模糊匹配标题命中正确 event_id（≥80% 相似）
4. curation YAML 可人工编辑、语法错误有明确报错且不阻断主报
5. 测试: `tests/test_curation.py`（应用逻辑、回写缓存、模糊匹配、YAML 容错）

### T-C2 · 发布前复核队列 `ai-daily review`（P2，~2-3 天）

**目标**: 把"高分/有 flag 的条目"先给用户复核再定稿：一键通过/退回，复核意见留痕。

**现状**: 事实核查的 quality_flags 只写进 runlog，没有人去看；高分条目直接发布。

**落地后**:
```bash
ai-daily review                       # 列出最近一期待复核条目（≥85分 或 有 flag）
ai-daily review --approve <id>        # 通过
ai-daily review --reject <id> --reason "…"   # 退回（标记后不入最终版）
ai-daily review status                # 复核进度
```
- 看板新增 `/review` 页（同一数据源）：待复核清单、通过/退回按钮、理由输入
- 复核结论存 `data/curation/review_{date}.yaml`；最终渲染时仅采纳"已通过"条目，"退回"条目降为 quality_flags 记录

**关键模块/文件**:
- 复用 T-C1 的 curation store + `app/curation/review.py`（复核状态机 pending→approved|rejected）
- `app/web/server.py` 增加 /review 路由（复用现有 handler 模式）

**验收标准**:
1. 一期待复核清单只含 高分(≥85) 或有 flag 条目，数量正确
2. approve/reject 后 review status 状态正确且持久化
3. 渲染时 rejected 条目不出现在最终版；approved 高亮"已复核"
4. 看板 /review 页可完成一次完整复核流（GET 列表 + POST 操作）
5. 测试: `tests/test_review.py`

---

## 六、主题 D · 内容形态与生命周期

### T-D1 · 3 分钟简报版 `ai-daily brief`（P2，~2 天）

**目标**: 全量日报之外，自动生成"3 分钟速览"：今日头条摘要 + 每栏目一句话 + 关键数字表，先给决策者看。

**落地后**: 出报后自动多产一份：
```
data/reports/{date}/daily_{date}_brief.md   # 300-400 字
daily_{date}_brief.html                     # 可点链接
daily_{date}_brief.pdf                      # 1-2 页
```
- 简报生成规则：头条 1-3 条标题+一句话摘要；每栏目取最高分条目一句话；关键数字（融资额/模型参数/增速）汇总成表；含"查看完整版"链接
- 用 LLM 生成一份总括段（可复用 Analyst 摘要，失败则模板拼接降级）；推送通道（T-A3 digest）直接复用 brief 内容

**关键模块/文件**:
- 新增 `app/report/brief.py`（~200 行，模板拼接 + 可选 LLM 润色）
- `run_daily.py` post_report_steps 增加 brief 导出；exporter 加 `brief` 格式

**验收标准**:
1. 由既有 daily JSON 生成 brief 三件套，字数 300-400，栏目全覆盖
2. LLM 不可用时模板降级仍可出 brief
3. 关键数字来自 key_data 字段，与全量版一致（可抽检）
4. 测试: `tests/test_brief.py`

### T-D2 · 公司库管理 `ai-daily companies`（P2，~1-2 天）

**目标**: 用命令增删跟踪公司，自动更新 529 条查询；并基于情报库推荐"近期高频出现但未跟踪"的公司。

**现状**: 公司名单写死在 `config/search_strategy.yaml`，加公司要手工改 yaml、还要确认查询矩阵生成正确；AI 行业新贵层出不穷，无人提醒。

**落地后**:
```bash
ai-daily companies list                # 已跟踪公司
ai-daily companies add "Anthropic"     # 加入跟踪（自动生成查询词，dry-run 预览新增查询）
ai-daily companies remove "Cohere"
ai-daily companies suggest             # 从 entities 表找：近30天出现≥N次但未跟踪的公司
```

**关键模块/文件**:
- `config/search_strategy.yaml` 公司段读改写（保留注释与排序）
- `app/search/queries.py` 增加 `render_company_queries(name)`（复用现有公司×事件查询模板）
- 新增 `app/companies.py`（管理 + suggest 逻辑，基于 entities 聚合）

**验收标准**:
1. `add` 后 yaml 出现该公司，`info` 显示查询数按预期增加；`remove` 反向
2. `suggest` 在有数据时给出候选（含出现次数），无数据时友好提示
3. yaml 读写不破坏已有结构与注释
4. 测试: `tests/test_companies.py`（增删改、查询生成、suggest、yaml 保真）

### T-D3 · 数据生命周期治理 `ai-daily prune|archive`（P2，~1 天）

**目标**: 一键归档压缩旧产物、清理版本残留、报告磁盘占用，防止 data/ 无限膨胀。

**现状**: data/raw 32MB + data/reports 124MB 只增不减；`_v2.._v11` 版本残留、嵌套目录（`2026-08-31/2026-08-31/`）需要定期人肉清理。

**落地后**:
```bash
ai-daily disk                          # 各目录占用 + 按日期分布
ai-daily archive --older-than 90       # 90天前产物移到 data/archive/（可 zip）
ai-daily prune --dry-run               # 预览可清理项：版本残留(*_vN)、临时文件、空目录
ai-daily prune                         # 确认后清理
```
- 安全规则：只动 `data/`；日报 JSON/PDF 主产物默认进 archive 而非删除；`--dry-run` 默认必看

**关键模块/文件**:
- 新增 `app/storage/lifecycle.py`（~200 行：scan/classify/archive/prune，全部标准库）
- 模式识别：`*_v\d+*`、`*.bak`、空目录、`{date}/{date}/` 嵌套

**验收标准**:
1. `disk` 输出真实 du 数据与日期分布
2. `archive --older-than 90 --dry-run` 列出正确文件清单；执行后原路径消失、archive 出现
3. `prune --dry-run` 只列 `_vN/.bak/空目录/嵌套`，不误列主产物
4. 全部操作可回滚（archive 保留原文件，prune 前生成清单）
5. 测试: `tests/test_lifecycle.py`（分类、dry-run、归档、嵌套目录识别）

---

## 七、锦上添花（O，可选，每项 ≤0.5 天）

| # | 主题 | 用户价值 | 实现 |
|---|------|---------|------|
| O-1 | `ai-daily setup` 交互式初始化 | 零基础用户 5 分钟跑通首报（生成 .env + doctor + 冒烟联动） | 复用 doctor/fallback |
| O-2 | 看板首页"完整出报"开关 | 当前 `_trigger` 只跑测试模式；支持选 `--full` | server.py 传参 |
| O-3 | `ai-daily diff <d1> <d2>` 跨期条目对比 | 内容审查（新增/消失/标题变化） | 复用标题相似度 |
| O-4 | 出报完成后本地通知 | 跑完叮一声（已具备 notify_local，接入主链路） | run_daily 末尾 |
| O-5 | `trend --format chart` | 趋势直接给 PNG 图（Pillow） | trend.py 扩展 |

---

## 八、codex 任务拆分与依赖

| Task | 主题 | 优先级 | 依赖 | 预估代码 | 落地周期 |
|------|------|--------|------|---------|---------|
| P0-A/B/C | 提交基线 + 固定环境 + 双验证 | P0 | 无 | 少量 | 0.5 天 |
| T-A1 | `ai-daily` 统一入口 | P0 | P0 基线 | 150-250 | 0.5-1 天 |
| T-A2 | 内置定时调度 | P0 | T-A1 | 250-400 | 2 天 |
| T-B1 | 公司档案与时间线 | P0 | 无（复用 entities 表） | 300-450 | 2-3 天 |
| T-A3 | 补跑 + 成功摘要推送 | P1 | T-A1 | 300-400 | 2 天 |
| T-B2 | 看板 SVG 图表 | P1 | T-B1(数据源可复用) | 250-350 | 2 天 |
| T-B3 | 精选 RSS 源摄入 | P1 | 无 | 250-350 | 2 天 |
| T-C1 | 人工编排覆盖 | P1 | T-B1(无强依赖) | 350-500 | 3 天 |
| T-C2 | 发布前复核队列 | P2 | T-C1 | 250-350 | 2-3 天 |
| T-D1 | 3 分钟简报版 | P2 | 无 | 200-300 | 2 天 |
| T-D2 | 公司库管理 | P2 | T-B1(建议可复用) | 200-300 | 1-2 天 |
| T-D3 | 数据生命周期治理 | P2 | 无 | 200-300 | 1 天 |

**推荐批序**（每批可独立验收、可回滚）:
- **批 1（地基+入口）**: P0-A/B/C → T-A1 → T-A2
- **批 2（情报核心）**: T-B1 → T-B2 → T-B3
- **批 3（协作+自动化）**: T-C1 → T-C2 → T-A3
- **批 4（形态+卫生）**: T-D1 → T-D2 → T-D3 → O-1~O-5

---

## 九、对 codex 的通用执行约束（沿用既有约定）

1. 一个 task 一个提交，commit 信息 `feat(T-XX): <一句话>`
2. 每个 task 必须：新增 ≥5 个单测 + `pytest` 全绿 + 更新 `使用实操文档.md` + 在 `架构评审与优化计划.md` 文末追加批次记录 + 运行记录写入 `runs/codex/<ts>/<TASK_ID>.log`
3. 标准库优先；不新增强依赖（已允许 Pillow/httpx 可选）
4. 只动本任务涉及文件，禁止顺手重构；失败不自动重试，写阻塞点等人工
5. 全程走 `--ask-for-approval on-request` + 不自动 push + 不在 main 分支提交
6. 每批落地后必须跑一次 `run_daily.py --test` 真实链路（沿用第 9 批发布检查单）

---

## 十、总评

前 10 批把系统从"能跑"推到了"跑得稳、用起来舒服"；第 11 批补的是**自动化闭环（自动跑+追得回）、情报纵深（公司档案+趋势图+一手源）、人机协作（编排覆盖+复核）** 三块。这三块做完，用户每天的真实动作会收敛为：**早上收到简报推送 → 打开看板看健康 → 偶尔改两条编排 → 其余全自动**。单体边界完全够用，无需架构拆分。

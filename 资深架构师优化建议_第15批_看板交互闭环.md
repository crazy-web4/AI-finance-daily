# AI 财经日报 · 第 15 批 · 看板交互闭环

> 承接 T13（看板读报/搜索/洞察/订阅）、T14（归档/翻页/下钻/公司线/周报页）、T14c（订阅推送闭环）。
> 本批解决「看板只能看、不能操作且操作无反馈」：触发出报后黑盒、订阅页配不了推送通道、周报需回命令行生成。

## 缺口（实测）
- 首页「触发出报」是 fire-and-forget 子进程，页面**无任何进度/结果反馈**，只能去翻 `logs/web_trigger.log`。
- `/subscriptions` 表单只有公司/栏目，T14c 新增的 `webhook`/`emails` 通道在网页上**无处配置**，也无法预览推送内容。
- `/weekly` 只能看在线聚合；离线周报（HTML/PDF）要回终端 `main.py weekly`，页面无生成入口。

## 落地项（T15）
- **T15-A 出报状态可见**：trigger 写 `logs/trigger_state.json`（pid/mode/started_at/log/date）；
  新增 `/api/trigger/status`（进程存活→running；结束→读当日最新 runlog 给 success/elapsed/阶段/flags）；
  首页嵌极简原生 JS（零依赖）每 5s 轮询，显示「运行中/成功/失败 + 耗时」徽章。
- **T15-B 订阅页增强**：表单加 `webhook` 与 `emails` 字段；新增「预览推送摘要」`POST /digest/preview`
  （用最新日报对该订阅做个性化过滤，返回将推送的 text/markdown 摘要，不发网络）；`/subscriptions` 列出每个订阅的通道。
- **T15-C 周报一键生成**：`/weekly` 页加「生成离线周报（HTML/PDF）」按钮，`POST /weekly/build?date=`
  走子进程 `main.py weekly --week-of`（PDF 用 playwright，慢，放后台），复用状态轮询提示。

## 约定与验证
- 路由薄接入 `app/web/server.py`；个性化/摘要复用 `personalization.digest` 与 `filter_report`；周报复用 `report.weekly.build_weekly`。
- 每项补 handler 测试；`scripts/pytest.sh -q` 全绿；实起 server curl 新路由 200。

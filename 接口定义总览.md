# AI 财经日报 · 五大接口定义总览

> 版本: v1.0.0
> 日期: 2026-08-19
> 状态: ✅ 已固化

---

## 一、接口关系图

```
┌─────────────────────────────────────────────────────────────┐
│                        IF-002                                │
│                   搜索策略配置                                │
│            config/search_strategy.yaml                       │
│         (30家公司 × 15类事件 + 行业 + 政策 + 论文)            │
└──────────────────────────────┬──────────────────────────────┘
                               │ 生成查询
                               ▼
┌─────────────────────────────────────────────────────────────┐
│                        IF-001                                │
│                   AnySearch API                              │
│              app/search/anysearch.py                         │
│        SearchQuery → SearchResultItem[]                      │
└──────────────────────────────┬──────────────────────────────┘
                               │ 原始搜索结果
                               ▼
┌─────────────────────────────────────────────────────────────┐
│                        IF-003                                │
│                   5 个 Agent Prompt                          │
│                      prompts/                                │
│                                                               │
│  01_researcher.md    → ResearchEvent                         │
│  02_fact_checker.md  → FactCheckedEvent                      │
│  03_classifier.md    → ClassifiedEvent                       │
│  04_editor.md        → ReportItem                            │
│  05_chief_editor.md  → DailyReport                           │
└──────────────────────────────┬──────────────────────────────┘
                               │ 中间数据结构
                               ▼
┌─────────────────────────────────────────────────────────────┐
│                        IF-004                                │
│                  JSON Schema 数据模型                         │
│               app/schemas/models.py                          │
│                                                               │
│  L1 SearchResultItem                                         │
│  L2 RawNewsArticle                                           │
│  L3 NewsEvent                                                │
│  L4 ResearchEvent        ← Agent-1                           │
│  L5 FactCheckedEvent     ← Agent-2                           │
│  L6 ClassifiedEvent      ← Agent-3                           │
│  L7 ReportItem           ← Agent-4                           │
│  L8 DailyReport          ← Agent-5 (最终产物)                │
└──────────────────────────────┬──────────────────────────────┘
                               │ DailyReport JSON
                               ▼
┌─────────────────────────────────────────────────────────────┐
│                        IF-005                                │
│                     PDF 模板                                  │
│                    templates/                                │
│              report.html + css/style.css                     │
│         (封面 + 目录 + 六大栏目 + 封底 + 水印)               │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、接口明细

### IF-001：AnySearch API 接口

| 项目 | 内容 |
|------|------|
| **文件** | `app/search/anysearch.py` |
| **职责** | 统一封装全网搜索能力，向上层提供标准化搜索结果 |
| **核心类** | `AnySearchClient`、`SearchQuery`、`SearchBatchRequest`、`SearchResultItem` |
| **输入** | 查询词、时间窗口、结果数、深度、语言、域名过滤 |
| **输出** | `SearchResultItem[]` —— 统一格式的搜索结果列表 |
| **替换引擎** | 只需改本文件，不影响上层 |
| **时间规范** | 全部 UTC + ISO 8601 |

**设计要点：**
- 上层只依赖 `SearchResultItem` 数据结构，不感知具体搜索引擎
- 支持单条搜索 + 批量并发搜索
- 结果自带 `result_id`、`query_origin`，便于追溯

---

### IF-002：搜索策略配置

| 项目 | 内容 |
|------|------|
| **文件** | `config/search_strategy.yaml` |
| **职责** | 定义"搜什么、怎么搜、搜多少"的全部规则 |
| **格式** | YAML 配置，改配置不碰代码 |
| **搜索批次** | 5 个批次（公司事件/行业趋势/政策监管/学术研究/中文媒体） |
| **公司覆盖** | 30 家核心 AI 公司（中美欧） |
| **事件类型** | 15 类（模型/融资/合作/芯片/政策…） |
| **行业主题** | 8 大方向（基础模型/算力/Agent/机器人/安全…） |

**搜索矩阵规模：**
- 第一批：30 公司 × 15 事件类型 = 450 条查询
- 第二~五批：约 40 条行业/政策/研究/中文查询
- 总计：约 500 条查询/天（可按权重分批执行）

**来源分级：** Tier1（官方） / Tier2（权威媒体） / Tier3（科技媒体）

---

### IF-003：Agent Prompt 定义

| 编号 | Agent | 文件 | 输入 | 输出 | 核心职责 |
|------|-------|------|------|------|----------|
| A1 | Researcher | `prompts/01_researcher.md` | 搜索结果聚类组 | `ResearchEvent` | 从多篇来源提炼结构化事件 |
| A2 | Fact Checker | `prompts/02_fact_checker.md` | ResearchEvent | `FactCheckedEvent` | 事实核查，数字/时间/主体/来源/逻辑 |
| A3 | Classifier | `prompts/03_classifier.md` | FactCheckedEvent[] | `ClassifiedEvent[]` | 六分类 + 重要性评分 + 话题标签 |
| A4 | Editor | `prompts/04_editor.md` | ClassifiedEvent + 完整数据 | `ReportItem` | 写成日报风格的新闻条目 |
| A5 | Chief Editor | `prompts/05_chief_editor.md` | ReportItem[] | `DailyReport` | 终审/排序/取舍/排版 |

**统一约束：**
- 所有 Agent 输出纯 JSON，无额外文字
- 输出结构严格对应 IF-004 的 Pydantic 模型
- 错误时通过 `passed: false` / `recommendation: reject` 等字段表达，不抛异常

---

### IF-004：JSON Schema 数据模型

| 项目 | 内容 |
|------|------|
| **文件** | `app/schemas/models.py` + `schemas/*.json` |
| **技术** | Pydantic v2，自动生成 JSON Schema |
| **层级** | 8 层数据结构，从搜索结果到最终日报 |
| **导出** | `python -m app.schemas.models --generate` |

**数据链路：**

```
L1 SearchResultItem   ← 搜索结果
   ↓ 归一化
L2 RawNewsArticle     ← 原始文章
   ↓ 去重聚类
L3 NewsEvent          ← 事件（多对一）
   ↓ Agent-1 研究员
L4 ResearchEvent      ← 结构化事件
   ↓ Agent-2 事实核查
L5 FactCheckedEvent   ← 核查后的事件
   ↓ Agent-3 分类器
L6 ClassifiedEvent    ← 已分类评分
   ↓ Agent-4 编辑
L7 ReportItem         ← 日报条目
   ↓ Agent-5 总编辑
L8 DailyReport        ← 最终日报 ✅
```

**关键枚举：**
- `Category`：六大栏目（top_news / model_tech / funding / policy / research / industry）
- `EventType`：17 种事件类型
- `SourceReliability`：high / medium / low / unknown
- `FactCheckRecommendation`：approve / revise / reject

---

### IF-005：PDF 模板

| 项目 | 内容 |
|------|------|
| **文件** | `templates/report.html` + `templates/css/style.css` |
| **渲染引擎** | Playwright + Chromium（print to PDF） |
| **页面尺寸** | A4（210mm × 297mm） |
| **字体** | Noto Sans CJK SC / Noto Serif CJK SC |
| **输入** | `DailyReport` 对象（IF-004 L8） |
| **输出** | 单文件 PDF |

**页面结构：**
1. **封面页** — 标题 / 日期 / 公司 / 微信号 / 金色装饰线
2. **目录页**（可选，超过5页显示）— 六大栏目导航
3. **栏目页 × 6** — 每个栏目独立起页
   - 栏目标题（大号数字 + 中文名 + 底部金线）
   - 新闻条目（序号/标题/导语/关键数据/详情/行业判断/来源）
4. **封底** — 公司信息

**视觉规范：**
- 主色：深蓝 `#1a365d` + 金色 `#c9a961`
- 正文字号：10.5pt，行高 1.7
- 条目标题：13pt 粗体
- 关键数据：浅灰底 + 左侧金色竖线
- 行业判断：浅黄底 + 左侧橙色竖线
- 水印：斜45°「明雯科技」，透明度 3.5%~5%
- 页脚：公司名 + 微信号 / 报告名 + 日期 / 页码

**部分模板：**
| 文件 | 用途 |
|------|------|
| `partials/cover.html` | 封面 |
| `partials/toc.html` | 目录 |
| `partials/header.html` | 页眉（备用） |
| `partials/footer.html` | 页脚（备用） |

---

## 三、文件结构（接口相关部分）

```
AI财经日报/
├── 接口定义总览.md              ← 本文件
│
├── app/
│   ├── search/
│   │   └── anysearch.py         ← IF-001 AnySearch API
│   │
│   ├── schemas/
│   │   └── models.py            ← IF-004 数据模型（Pydantic）
│   │
│   └── report/
│       └── renderer.py          ← IF-005 渲染器接口
│
├── config/
│   └── search_strategy.yaml     ← IF-002 搜索策略配置
│
├── prompts/
│   ├── 01_researcher.md         ← IF-003 Agent-1
│   ├── 02_fact_checker.md       ← IF-003 Agent-2
│   ├── 03_classifier.md         ← IF-003 Agent-3
│   ├── 04_editor.md             ← IF-003 Agent-4
│   └── 05_chief_editor.md       ← IF-003 Agent-5
│
├── schemas/                     ← IF-004 JSON Schema 导出
│   ├── SearchResultItem.json
│   ├── RawNewsArticle.json
│   ├── NewsEvent.json
│   ├── ResearchEvent.json
│   ├── FactCheckedEvent.json
│   ├── ClassifiedEvent.json
│   ├── ReportItem.json
│   └── DailyReport.json
│
└── templates/                   ← IF-005 PDF 模板
    ├── report.html              ← 主模板
    ├── css/
    │   └── style.css            ← 全部样式（539行）
    └── partials/
        ├── cover.html           ← 封面
        ├── toc.html             ← 目录
        ├── header.html          ← 页眉
        └── footer.html          ← 页脚
```

---

## 四、接口之间的契约边界

| 接口 | 上游 | 下游 | 约定 |
|------|------|------|------|
| IF-001 | 搜索策略 / 外部API | collector.py | 只输出 `SearchResultItem`，不做内容理解 |
| IF-002 | 配置文件 | 查询生成器 | YAML 变更即生效，无需改代码 |
| IF-003 | 各阶段数据 | 下一阶段 Agent | 纯 JSON 输出，结构对得上 schema |
| IF-004 | 全部 Agent | PDF 渲染器 | Pydantic 模型是唯一真实来源 |
| IF-005 | DailyReport | 文件系统 / 推送 | 输入是 DailyReport 对象，输出是 PDF 文件 |

---

## 五、下一步（V1 剩余工作）

接口已全部定义完成。要让整个流程跑起来，还需要实现：

1. **查询生成器** — 读取 IF-002 配置，生成实际查询列表
2. **采集器** — 调用 IF-001，把结果存入候选池
3. **去重聚类器** — URL去重 → 标题去重 → 语义聚类
4. **Agent 调用层** — 把 IF-003 的 prompt 接到 LLM API
5. **PDF 渲染实现** — 把 IF-005 的模板用 Playwright 实际渲染
6. **调度器** — cron 触发整个流水线
7. **存储层** — SQLite 持久化

# AI 财经日报 · 第 14 批 · 看板导航与阅读纵深

> 承接第 13 批（Web 看板产品化：在线读报/搜索/洞察/订阅已上线，266 测试全绿）。
> 本批解决「读报时的纵深与流动」：能找到历史、能跳转日期、能深挖单条、能顺着线索读相关。

## 现状缺口（实测）
- 首页仅列近 8 期，**13 期历史无归档入口**，老日报只能改 URL 访问。
- 读报页无「上一期/下一期」，看完一期要回首页。
- 条目只是平铺，**无目录、无锚点**，长报难定位；想深挖某条的来源/相关事件没有下钻页。
- 搜索/洞察里看到感兴趣的公司，无法一键看「该公司全部相关报道」。

## 落地项（T14）
- **T14-A 历史归档** `/archive`：全部期数按月分组列表（日期/条目数/成功标记/产物/PDF），首页「查看全部 N 期」入口。
- **T14-B 读报导航**：`/read/<date>` 顶部加上一期/下一期按钮 + 本期目录（栏目锚点跳转）；条目标题加锚点 id。
- **T14-C 条目下钻** `/read/<date>#item_xxx` 锚点已可用；新增 `/item/<date>/<item_id>` 单条深度页：
  标题/关键数据/正文/点评/全部来源（可点击）+「该公司/关键词的跨期相关报道」（复用 search_conn）。
- **T14-D 公司时间线** `/company/<name>`：聚合该公司全部历史条目（日期/栏目/重要度/标题），
  从条目页与高频公司标签一键进入；复用 search_conn（LIKE 召回）。
- 渲染纯函数集中进 `app/web/readview.py`，路由薄接入；每项补 handler 测试。

## 验证
`scripts/pytest.sh -q` 全绿 + 实起 server curl 新路由 200 + `main.py export` 确定性渲染通过。

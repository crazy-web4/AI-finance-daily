# T-D12 — 优雅降级与依赖预检(`ai-daily doctor --strict`)

> 任务来源:`资深架构师优化建议_产品化.md` §#12 · 批次:1(地基)
> 优先级:P0 · 预估代码量:500~700 行 · 依赖:T-A3(可选)

## 任务定义

- **文档来源**:`资深架构师优化建议_产品化.md` §二.12
- **目标**:LLM 临时不可用 / playwright 缺失 / 额度耗尽时,优雅降级而不直接崩
- **现状**:LLM 限流/缺 playwright/额度耗尽 → 直接崩,无提示
- **落地后**:
  - 依赖预检:跑前 doctor 自检
  - **LLM 降级**:LLM 不可用 → 模板生成(更短但能出报);部分失败 → 成功入选 + quality_flags 告警
  - **PDF 降级**:playwright 未装 → 自动 fallback 到 weasyprint / 仅输出 Markdown + HTML
  - **额度预警**:AnySearch/Tavily 报 429 → 自动降级到摘要;LLM 报 429 → 启动降级路径

## 涉及文件(必动)

- 新建:`/Users/weike/IdeaProjects/cmjt/0-应用创新/response-api/AI财经日报/app/utils/fallback.py`(~250 行)
- 新建:`/Users/weike/IdeaProjects/cmjt/0-应用创新/response-api/AI财经日报/app/utils/ratelimit.py`(~150 行)
- 新建:`/Users/weike/IdeaProjects/cmjt/0-应用创新/response-api/AI财经日报/tests/test_fallback.py`(≥5 个)
- 新建:`/Users/weike/IdeaProjects/cmjt/0-应用创新/response-api/AI财经日报/tests/test_ratelimit.py`(≥3 个)
- 修改:`/Users/weike/IdeaProjects/cmjt/0-应用创新/response-api/AI财经日报/app/agents/pipeline.py`(关键步骤加 try/except + 降级)
- 修改:`/Users/weike/IdeaProjects/cmjt/0-应用创新/response-api/AI财经日报/app/report/renderer.py`(PDF 失败 → fallback Markdown)
- 修改:`/Users/weike/IdeaProjects/cmjt/0-应用创新/response-api/AI财经日报/使用实操文档.md`
- 修改:`/Users/weike/IdeaProjects/cmjt/0-应用创新/response-api/AI财经日报/架构评审与优化计划.md`

## 落地步骤

1. **`ratelimit.py`**:
   - `RateLimitDetector`:识别 429 / 配额耗尽 / 鉴权失效错误
   - `@retry_on_429(max_retries=3, backoff=60)`:装饰器,自动等待重试
2. **`fallback.py`**:
   - `fallback_llm_to_template()`:LLM 失败 → 用 jinja2 模板生成简短日报
   - `fallback_pdf_to_html()`:playwright 未装 → 用 weasyprint(可选依赖)/ 仅输出 Markdown + HTML
   - `quality_flags`:在日报 JSON 加 `quality_flags: ["llm_failed"]` 等,前端可提示
3. **`pipeline.py` 改造**:
   - LLM 调用包 `@retry_on_429`,重试 3 次仍失败 → 走 fallback
   - PDF 渲染 try/except,失败 → 写 quality_flags
4. **`renderer.py` 改造**:检测 playwright 可用性,优先 weasyprint,最后纯 HTML
5. **测试**:
   - mock LLM 抛 429 → 自动重试 + 降级,产物含 quality_flags
   - mock playwright 缺失 → 输出 HTML/MD,terminal 提示安装
   - 429 连续 3 次后降级
   - 部分 LLM 成功:成功的入选,失败的标占位
   - 额度预警触发降级
6. **commit**:`feat(T-D12): graceful fallback for LLM/PDF/limits`

## 验收清单

- [ ] mock LLM 抛 429 → 3 次重试后降级,日报仍产出,quality_flags 含 `llm_failed`
- [ ] mock playwright 未装 → 自动 fallback 到 Markdown + HTML,终端提示安装
- [ ] mock 部分 LLM 失败 → 成功入选,失败标占位
- [ ] pytest 全绿(123 + ≥8 个)
- [ ] 使用实操文档.md 加 §"优雅降级"
- [ ] 架构评审与优化计划.md 追加
- [ ] commit:`feat(T-D12): graceful fallback for LLM/PDF/limits`
- [ ] log 完整

## 禁止项

- 不要自动 push
- 不要硬降级而隐藏错误(LLM 失败必须 quality_flags 标记)
- 不要引入新依赖(weasyprint 标记 optional)
- 不要顺手改其他模块

## 失败处理

- 验收不过 → log 留痕,不 commit

## 完成后输出

```
=== T-D12 Report ===
diff stat: ...
pytest 末 30 行: ...
commit hash: ...
总结: <1 句话>
```

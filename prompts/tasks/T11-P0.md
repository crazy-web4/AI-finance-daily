# T11-P0 — 基线固化：提交第 10 批未提交改动 + 固定解释器/依赖 + 双验证

> 任务来源: `资深架构师优化建议_第11批_单体深化.md` §二 · 批次:1(地基)
> 优先级:P0 · 预估代码量:少量 + 1 个 commit · 依赖:无

## 任务定义

- **文档来源**:`资深架构师优化建议_第11批_单体深化.md` §二 P0-A/B/C
- **目标**:在开工前把第 10 批改动提交固化，固定单一 Python 解释器与依赖，建立每批落地的双验证约定
- **现状**:
  - 第 10 批全部功能（web/export/cards/feed/index/trend/weekly/watch/subscribe）代码在工作区**未提交**（12 个文件、+703/-112 行），dev 分支领先 origin/dev 1 个提交
  - 3.12 / 3.14 双解释器并存，playwright 只在其中一个可用（第 9 批 E1 已踩坑：跑完 40 分钟才在 PDF 阶段崩）
  - pytest 依赖与解释器绑定不清晰
- **落地后**:基线干净、可回滚；`doctor` 能校验解释器/依赖一致性；每批提交后强制 `pytest` + `run_daily.py --test` 双验证

## 涉及文件(必动)

- 修改:`/Users/weike/IdeaProjects/cmjt/0-应用创新/response-api/AI财经日报/.gitignore`
- 新建:`/Users/weike/IdeaProjects/cmjt/0-应用创新/response-api/AI财经日报/requirements.txt`
- 新建:`/Users/weike/IdeaProjects/cmjt/0-应用创新/response-api/AI财经日报/AGENTS.md`(若不存在;存在则补充"双验证"约定)
- 修改:`/Users/weike/IdeaProjects/cmjt/0-应用创新/response-api/AI财经日报/app/utils/doctor.py`(解释器/venv/依赖一致性检查项)
- 修改:`/Users/weike/IdeaProjects/cmjt/0-应用创新/response-api/AI财经日报/使用实操文档.md`
- 修改:`/Users/weike/IdeaProjects/cmjt/0-应用创新/response-api/AI财经日报/架构评审与优化计划.md`
- 测试: 新建/修改 `/Users/weike/IdeaProjects/cmjt/0-应用创新/response-api/AI财经日报/tests/test_doctor.py`(新增解释器检查用例)

## 落地步骤

1. **先冻结现状**:记录 `git status`、`git diff --stat`、当前解释器版本到 run log（不删任何东西）
2. **提交第 10 批未提交改动**:`git add -A && git commit -m "feat(T11-P0): 提交第10批产品化12条落地"`,记录 commit hash
3. **固定依赖**:`pip3.12 freeze` 或按 import 梳理生成 `requirements.txt`(锁版本),`pip install -r requirements.txt` 验证可装
4. **解释器一致性**:`doctor.py` 新增检查项 —— 检测当前 python 是否有 pytest/playwright,若无给出"用 venv 或固定解释器"的修复建议;`run_daily.py` 启动时预检 playwright 可用性并提前告警(而非尾部崩溃)
5. **双验证约定**:在 `AGENTS.md` 补充 —— 每批 codex 提交后必须 `python3.12 -m pytest -q` 全绿 + `python3.12 run_daily.py --test` 真实链路跑通(缺 playwright 时明确记录)
6. **测试**:`tests/test_doctor.py` 新增 ≥3 个用例(解释器检查、依赖缺失提示、venv 检测)
7. **commit**:`feat(T11-P0): 基线固化与双验证约定`

## 验收清单

- [ ] `git status` 干净(第 10 批改动已提交),记录基线 commit hash
- [ ] `requirements.txt` 存在且可 `pip install -r` 安装
- [ ] `doctor` 输出含"解释器/依赖一致性"检查项,缺失时给出可执行修复命令
- [ ] `run_daily.py --test` 在缺 playwright 时提前告警而非跑完全部才崩(用 mock 验证逻辑)
- [ ] AGENTS.md 含"pytest + run_daily --test 双验证"约定
- [ ] pytest 全绿(243 + 新增)
- [ ] 使用实操文档.md 更新(解释器/venv 说明)
- [ ] 架构评审与优化计划.md 追加批次记录
- [ ] commit 信息:`feat(T11-P0): 基线固化与双验证约定`
- [ ] log 完整

## 禁止项

- 不要删除任何 data/ 下的产物
- 不要 `git push`(除非用户显式要求)
- 不要改动批 10 已实现的功能逻辑(只提交+补检查项)

## 失败处理

- 提交前若有未预期的大改动,停下来在 log 写清单,不擅自提交
- 验收不过 → log 留痕,不 commit

## 完成后输出

```
=== T11-P0 Report ===
基线 commit: ...
diff stat: ...
pytest 末 30 行: ...
commit hash: ...
总结: <1 句话>
```

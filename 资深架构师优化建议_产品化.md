# AI 财经日报 · 资深架构师产品化优化建议（第 10 批 · 单体深化）

> 评审日期: 2026-09-05 ｜ 评审基线: dev @ 第 9 批(123 测试全绿、9 轮架构评审已落地)
> 评审范围: 全部源码(约 5500 行)+ 配置 + 模板 + 66 份历史产出 + 历次评审文档
> 性质: 与前 9 批的"修 bug / 收敛配置 / 加测试"性质不同——本批面向 **产品化跃迁**,聚焦"用户每天与系统交互的真实感受"。

## 〇、与前 9 批的关键区别

| 维度 | 前 9 批 | 本批 |
|------|---------|------|
| 目标 | 让日报"稳定跑、跑正确" | 让用户"用得舒服、用得多" |
| 性质 | 内部质量/架构 | 用户体验/产品化 |
| 验证标准 | 单元测试 / 全链路 `--test` | 用户场景 / 体验路径 |
| 单次代码量 | 10~300 行 | 通常 300~1500 行/任务 |
| 验收节奏 | 提交即测 | 每个建议自带 demo 路径 |

**核心原则**: 一切建议都在单体边界内,通过 CLI 子命令 / 单页 Web / 新增模块实现,无需拆分微服务。

---

## 一、总体方向(用户视角的"今天 vs 明天")

### 1.1 现状痛点(用户每天的真实摩擦)

| # | 摩擦 | 用户场景 | 痛点 |
|---|------|---------|------|
| F1 | 看不到历史 | "上周末我那份日报在哪?" | `data/reports/` 散落 66 份产出,无索引 |
| F2 | 不知道系统是否健康 | "昨天 cron 跑成功了吗?" | `run_*.json` 在 daily子目录,要看就 grep |
| F3 | 配置不友好 | "我想加一家新公司,改 yaml 总会报错" | yaml 改错就 yaml 反序列化失败,无 schema 校验 |
| F4 | 没有快速通道 | "今天这份 PDF 直接给我!" | 必须 `python run_daily.py --full ...` 等 30 分钟 |
| F5 | 报告不可二次消费 | "我想把日报导出到 Notion / 邮件 / 公众号" | 只能取 PDF,JSON 也只是给程序看的 |
| F6 | 单调一致 | "能不能给我英文版 / 简报版 / 卡片版" | 每次都是同一份中文 PDF |
| F7 | 不沉淀 | "OpenAI 这周都做了什么?" | 数据没建索引,只能人肉翻 PDF |
| F8 | 不进化 | "上周说融资是热点,这周热度下降应该自动告警" | 系统只管出报,不管"读报"反馈 |
| F9 | 无优雅降级 | "LLM 挂了/额度用完?" | 直接崩,无兜底 |
| F10 | 无交付物清单 | "我交付的日报到底包含哪些条目?" | 终端一段流水,无法给他人看 |

### 1.2 本批要解决的 Top 4 方向

| 方向 | 用户价值 | 涉及建议 |
|------|---------|---------|
| **A · 一站式看板** | 打开浏览器即可看历史、看健康、手动触发 | #1 本地 Web 看板、#2 今日报告快捷命令、#3 健康巡检 |
| **B · 多形态交付物** | PDF 外多一份 Markdown / 公众号 / 邮件 / 卡片 | #4 多格式输出、#5 衍生卡片图、#6 本地 RSS / Feed |
| **C · 知识沉淀** | 日报积累成可检索的本地情报库 | #7 SQLite 索引、#8 跨期对比与趋势、#9 周报自动聚合 |
| **D · 主动服务** | 不只是出报,而是"读报反馈+主动告警+订阅" | #10 异常告警增强、#11 订阅与个性化、#12 优雅降级与依赖预检 |

---

## 二、12 条具体建议(产品化)

每条建议的字段统一为:
- **目标**: 用户能感知到的具体好处(一句话)
- **现状**: 今天要做什么
- **落地后**: 用户能做什么
- **关键模块/文件**: 涉及的关键位置
- **验收标准**: codex 做完如何自检
- **代码量预估**: 让 codex 估算工时

---

### #1 · 本地 Web 看板(`ai-daily web`)

**目标**: 浏览器打开 `http://127.0.0.1:8910` 即可看到历史日报、运行健康、配置状态。

**现状**:
- 看历史要 `ls data/reports/`、`cat data/reports/{date}/run_*.json`
- 看运行健康要 grep runlog
- 看今天的 PDF 要 `open data/reports/{date}/...pdf`(命令长)

**落地后**:
- 浏览器打开 → 首页:今日报告卡片 + 近 7 天列表(缩略图、字数、状态)
- 点击某日报 → 查看 PDF/HTML/JSON 三种产物,以及该次运行的 runlog
- "运行健康"页:近 30 天成功率、平均耗时、缓存命中率、LLM 调用成本
- "触发"按钮:点击即启动一次端到端出报(后台线程跑,不阻塞页面)
- "配置"页:可视化显示当前配置(.env / yaml 关键字段),可一键校验

**关键模块/文件**:
- 新增 `app/web/`(单文件 FastAPI/Starlette 应用,~400 行):
  - `app/web/server.py`:ASGI 入口
  - `app/web/routes.py`:路由
  - `app/web/templates/index.html`:首页
  - `app/web/static/`:CSS/JS
- 新增 CLI 子命令:`python main.py web --port 8910`
- 不引入新依赖:用 **Python 标准库 `http.server` + Jinja2**(避免 FastAPI 重依赖);或者可选 `starlette + uvicorn`(轻量)

**验收标准**:
1. `python main.py web --port 8910` → 浏览器访问首页能列出 `data/reports/{date}/daily_*.json`
2. 点击某日报 → 显示其 PDF 路径(供下载)+ HTML 预览 + runlog 摘要
3. "健康"页 → 30 天成功率、平均耗时(读 `run_*.json` 聚合)
4. "配置"页 → 显示当前 LLM 模型/时区/缓存配置(读 `app.config` + `app.search.queries.load_strategy`)
5. 关闭 web 进程时不留下孤儿线程;无 Python 致命错误
6. 新增测试 `tests/test_web.py`(不启动 server,只测路由 handler)

**代码量预估**: 800~1200 行(主代码) + 200 行(测试)

---

### #2 · "今日报告"快捷命令(`ai-daily today` / `ai-daily latest`)

**目标**: 一行命令打开今天的(或最近的)日报 PDF。

**现状**:
- 要查今天的 PDF 路径 → `ls data/reports/{today}/AI行业...pdf`(文件名很长,日期参数易错)

**落地后**:
```bash
python main.py today              # 自动用系统默认 PDF 阅读器打开今天的报告
python main.py today --open       # 等价于上面的 macOS `open` / Linux `xdg-open`
python main.py latest             # 打开最近一次产出的 PDF(无论日期)
python main.py latest --json      # 在终端打印最近一次日报 JSON 摘要
python main.py latest --stats     # 打印最近一次 runlog 的核心数字(文章数/事件数/耗时/LLM 调用数)
```

**关键模块/文件**:
- 新增 `app/cli/shortcuts.py`(命令集合,~150 行)
- 在 `main.py` 注册子命令 `today` / `latest`

**验收标准**:
1. `python main.py today` 终端打印"今日报告: data/reports/{today}/...pdf",并尝试 `open` 唤起默认应用
2. `python main.py latest` 在无今日日报时找最近一日,终端打印
3. `python main.py latest --json` 打印日报 JSON(指定 section/limit)
4. 无任何日报时给出友好提示并退出码 0(非错误)
5. 测试 `tests/test_cli_shortcuts.py`

**代码量预估**: 150~250 行

---

### #3 · 一行健康检查(`ai-daily doctor`)

**目标**: 一行命令自检整个系统是否可跑,主动给出修复建议。

**现状**:
- 跑 `run_daily.py --full` → 中途崩才发现 key 过期、playwright 没装、端口冲突……
- 错了一堆 traceback,普通用户看不懂

**落地后**:
```bash
python main.py doctor
# 输出:
# ✅ Python 3.14.0 (需要 ≥3.10)
# ✅ .env 中 ANYSEARCH_API_KEY 已配置
# ✅ playwright 已安装,chromium 已下载
# ⚠️  LLM 配置:仅 OPENAI_API_KEY,未配 ARK_*;推荐两者都配(降级)
# ⚠️  Tavily 未配置:补充搜索将自动跳过(无影响)
# ✅ data/raw 最近 7 天有产出
# ✅ data/reports 最近 7 天有 PDF
# ✅ 缓存目录 data/cache 写入正常
# ✅ runlog 最近 7 天 7/7 成功
# ─────────
# 总评:健康 ✓
```

**关键模块/文件**:
- 新增 `app/utils/doctor.py`(自检器,~250 行)
- 新增 CLI 子命令 `doctor`

**自检清单**(每个都有友好提示):
1. Python 版本 ≥3.10
2. `.env` 存在,`ANYSEARCH_API_KEY` 非空
3. 至少一组 LLM 配置(`ARK_*` 或 `OPENAI_*`)
4. `playwright` 已安装且 chromium 已下载(子命令 `playwright install --dry-run` 不行,所以写文件探测)
5. `data/raw/{today}/` 与 `data/reports/{today}/` 存在(过去 7 天是否有产出)
6. `data/cache/` 写入权限
7. `logs/` 写入权限
8. `run_*.json` 最近 7 天成功率 ≥80%
9. 网络可达性:`ping api.ark.cn-beijing.volces.com`(可选,不阻塞)

**验收标准**:
1. 在健全环境下 → 全部 ✅,退出码 0
2. 在缺失 playwright → 给出 `pip install playwright && playwright install chromium` 提示,退出码 1
3. 在 key 缺失 → 给出 `.env.example` 复制路径,退出码 1
4. 测试 `tests/test_doctor.py`(mock 各子系统状态)

**代码量预估**: 300~400 行

---

### #4 · 多格式输出(`ai-daily export`)

**目标**: 一份日报可同时输出 PDF / Markdown / 公众号富文本 / HTML 邮件版 / Notion 导入 JSON。

**现状**:
- 跑完只能拿到 PDF + JSON,JSON 是给程序看的,人想"二次消费"(贴到公众号/发邮件)只能从 PDF 复制

**落地后**:
```bash
# 自动在 PDF 之外多生成几份(默认行为)
python run_daily.py --full              # 现在会输出 PDF + MD + 公众号版 + 邮件版
# 单独导出某格式
python main.py export --date 2026-09-05 --format markdown
python main.py export --date 2026-09-05 --format wechat     # 公众号:每条事件一张图,带封面
python main.py export --date 2026-09-05 --format email      # 邮件版 HTML(无图版式)
python main.py export --date 2026-09-05 --format notion      # Notion 块 JSON(可直接 paste)
python main.py export --date 2026-09-05 --all                # 一次性导出全部格式
```

**关键模块/文件**:
- 新增 `app/exporter/` 模块:
  - `app/exporter/markdown.py`(~100 行):日报 → Markdown
  - `app/exporter/wechat.py`(~200 行):日报 → 公众号 HTML 块(标题+封面+每事件一段)
  - `app/exporter/email.py`(~150 行):日报 → 邮件 inline HTML
  - `app/exporter/notion.py`(~150 行):日报 → Notion 块 JSON
- 在 `run_daily.py` 渲染 PDF 后自动调用 `run_all_exporters()`
- `RenderConfig` 增加 `enabled_exporters` 字段(默认全开)

**验收标准**:
1. `--full` 跑完后,`data/reports/{date}/` 下新增 4 个文件:`*.md` / `*_wechat.html` / `*_email.html` / `*_notion.json`
2. Markdown 用标准 GFM,可被 Typora / VSCode / GitHub 正确渲染
3. 公众号版:每事件独立段落,字号限制内,图片 alt 完整
4. 邮件版:无外部依赖资源(inline CSS),可用 Gmail 直接发送
5. Notion 版:blocks 数组,可直接 `curl -X POST` 到 Notion API 创建页面
6. 关闭 `RenderConfig.enabled_exporters` 中某项 → 该文件不生成
7. 测试 `tests/test_exporters.py`(每种格式 golden sample)

**代码量预估**: 700~900 行

---

### #5 · 衍生卡片图(`ai-daily cards`)

**目标**: 每条事件一张 1080×1080 可分享卡片图(朋友圈/微博/小红书用)。

**现状**:
- 公众号发布通常会做"每条一张图",但目前要手工从 PDF 截图或重新设计

**落地后**:
```bash
python main.py cards --date 2026-09-05
# 生成:data/reports/{date}/cards/01_xxx.png ... 06_xxx.png ... (按栏目分目录)
# 共 ~30 张卡片(每事件一张),适合直接发朋友圈
python main.py cards --date 2026-09-05 --style dark  # 暗色主题
python main.py cards --date 2026-09-05 --only top    # 只生成头条卡片
```

**关键模块/文件**:
- 新增 `app/exporter/cards.py`(~300 行):
  - 使用 **Pillow**(已常用 or 需 `pip install Pillow`)绘制 PNG
  - 模板:头条卡片 / 普通事件卡片 / 关键数据卡片 / 引用金句卡片
- 模板可扩展:用户自定义 `--template-file`

**验收标准**:
1. 跑一次 → 生成 N 张 PNG(头条 1 张/事件,关键数据多的事件再加 1 张)
2. 卡片 1080×1080,带项目品牌水印(弱)
3. 文字超长自动折行,关键数据(图)显式标出
4. 测试 `tests/test_cards.py`(验证图片尺寸、文字存在、文件可读)

**代码量预估**: 400~500 行

---

### #6 · 本地 RSS / Atom Feed(`ai-daily feed`)

**目标**: 日报自动生成 RSS / Atom,订阅器(Feedly / NetNewsWire / Reeder)可订阅。

**现状**:
- 想"每天 8 点打开订阅器看昨天日报",但日报是 PDF/HTML 文件,无法被 RSS 阅读器识别

**落地后**:
```bash
python main.py feed --port 8911     # 起一个静态文件服务,暴露 feed.xml + 历史 HTML
# 输出:
#   http://127.0.0.1:8911/feed.xml
# 在 NetNewsWire 添加订阅 → 每天自动拉取日报标题/链接,点进去看完整 PDF
```

**关键模块/文件**:
- 新增 `app/web/feed.py`(~200 行):
  - 扫 `data/reports/{date}/daily_*.json` → 生成 RSS 2.0 + Atom 1.0 双格式
  - 每条 item 含:title(头条 1~3 条标题)/pubDate/link(本地路径)/description(摘要)
- 在 `main.py web` 子命令下挂上 `/feed.xml`
- 不引入依赖:用 Python 标准库 `xml.etree.ElementTree`

**验收标准**:
1. 浏览器访问 `http://127.0.0.1:8911/feed.xml` → RSS XML,包含近 30 天日报
2. NetNewsWire 添加订阅 → 列表显示日报标题,点击进入 HTML 预览页
3. 无任何日报时生成空 feed(只 metadata)
4. 测试 `tests/test_feed.py`(feed.xml 结构、必填字段)

**代码量预估**: 200~300 行

---

### #7 · SQLite 情报数据库(`ai-daily index`)

**目标**: 日报积累成可检索的本地情报库,"OpenAI 最近 30 天发生了什么" 一查便知。

**现状**:
- 历史 66 份日报躺在 `data/reports/`,只能翻文件名找,无法跨期检索

**落地后**:
```bash
python main.py index build                       # 一次性建立索引(扫 data/reports/)
python main.py index rebuild                      # 重新建(新数据进来后)
python main.py query "OpenAI" --days 30           # 全文检索
python main.py query "融资" --category funding    # 按栏目
python main.py query "Claude 4" --importance-min 85
python main.py stats --since 2026-08-01           # 统计:期数/总条目/按公司分布
```

输出:
```
$ python main.py query "OpenAI" --days 30
[2026-08-31] 模型 · 91 分 · "OpenAI 发布 GPT-5 编码版" —— 命中 3 处
[2026-08-30] 资本 · 87 分 · "OpenAI 完成 65 亿美元融资" —— 命中 5 处
...
```

**关键模块/文件**:
- 新增 `app/storage/db.py`(SQLite + FTS5,~350 行):
  - 表:`reports`(日期/标题/字数/路径)、`items`(事件,全文 FTS)、`entities`(公司/人物,跨期聚合)
  - 使用 SQLite 内置 FTS5(无需额外依赖)
- 新增 `app/storage/indexer.py`(~150 行):把日报 JSON 写进 DB
- 新增 `app/storage/query.py`(~200 行):CLI 查询层
- 在 `run_daily.py` 渲染完成后自动写库

**验收标准**:
1. `index build` 把现有 66 份日报全部入库,`data/intel.db` 大小合理(< 50MB)
2. `query "OpenAI" --days 30` 命中条数正确,中文分词正确(用 `unicode61` + 自定义 `tokenchars='%'`)
3. `stats --since 2026-08-01` 给出按公司、按栏目、按重要性的分布
4. 增量:`run_daily.py --full` 跑完后自动入库(无需手动 rebuild)
5. 测试 `tests/test_db_indexer.py` + `tests/test_db_query.py`

**代码量预估**: 700~900 行

---

### #8 · 跨期对比与趋势(`ai-daily trend`)

**目标**: "这周融资热点是哪些公司?""上周 vs 本周头条差异" 一查便知。

**现状**:
- 要看趋势只能手工翻 N 份 PDF

**落地后**:
```bash
python main.py trend funding --weeks 4           # 近 4 周融资热点公司 TOP 10
python main.py trend headlines --diff last-week  # 本周 vs 上周头条差异
python main.py trend topics --weeks 12           # 近 12 周话题热度曲线
```

输出:
```
$ python main.py trend funding --weeks 4
近 4 周融资热点:
  1. OpenAI  8 次 · 累计 $32B
  2. Anthropic  6 次 · 累计 $18B
  3. xAI  5 次 · 累计 $11B
  ...

$ python main.py trend headlines --diff last-week
本周新增头条:
  + OpenAI GPT-5 编码版(91 分)
  + Anthropic 完成 $18B 融资(89 分)
上周头条已下榜:
  - Meta 开源 Llama 4(85 分)→ 本周未入选头条
```

**关键模块/文件**:
- 新增 `app/storage/trend.py`(~250 行):
  - 基于 #7 的 SQLite 索引做聚合 SQL
- 新增 `app/cli/trend.py`(~150 行):CLI 层
- 可选:`--format chart` → 输出 PNG 折线图(用 Pillow,无 matplotlib 依赖)

**验收标准**:
1. `trend funding --weeks 4` 在有 4 周数据时返回正确 TOP 列表
2. `trend headlines --diff last-week` 区分"新增/保留/下榜" 三类
3. `trend topics --weeks 12` 输出话题热度序列
4. 测试 `tests/test_trend.py`

**代码量预估**: 400~500 行

---

### #9 · 周报自动聚合(`ai-daily weekly`)

**目标**: 周日自动生成"本周回顾"——把 7 天日报合并为 1 份周报(深度研究版)。

**现状**:
- 每天出日报,但"一周回顾"需要人肉做

**落地后**:
```bash
python main.py weekly --week-of 2026-08-31     # 生成以该周任一天为锚的周报
# 输出:data/reports/weekly/{week_start}/周报_2026-08-31_to_2026-09-06.pdf
```

**关键模块/文件**:
- 新增 `app/report/weekly.py`(~400 行):
  - 加载本周 7 份 daily JSON
  - 按公司/主题聚合,生成"本周 TOP 10 公司动态 / 本周 TOP 5 融资 / 本周模型发布节奏 / 本周政策大事记"
  - 复用 `templates/weekly.html` 模板(新增)
- 新增 `templates/weekly.html` + `templates/css/weekly.css`

**验收标准**:
1. `weekly --week-of 2026-08-31` 跑通,生成 1 份 PDF(8~15 页)
2. 周报内容:本周总条目数 / 头条合并去重 / 主题热度 TOP 5 / 公司活跃度 TOP 10 / 本周关键政策 / 模型发布时间轴
3. 复用现有 PDFRenderer(传入新模板名)
5. 测试 `tests/test_weekly.py`(基于 mock 数据集验证聚合逻辑)

**代码量预估**: 500~700 行

---

### #10 · 异常告警增强(`ai-daily watch`)

**目标**: 跑日报时**主动**发现异常:某公司突然爆火、某话题热度异常、采集量骤降,主动通知。

**现状**:
- `send_alert()` 仅在最终失败时告警,中间阶段无主动检测

**落地后**:
```bash
python main.py watch                             # 持续监控(配合 cron/launchd)
# 配置:data/watch.yaml
# - on_company_spike: OpenAI 单日事件数 >4 倍周均 → 告警
# - on_topic_drop: 某栏目连续 3 天 0 条 → 告警
# - on_collection_drop: 今日采集量 < 昨日 50% → 告警
```

**关键模块/文件**:
- 新增 `app/utils/anomaly.py`(~300 行):基于 #7 SQLite 索引的异常检测
- 新增 `config/watch.yaml`(规则定义)
- 在 `run_daily.py` 各阶段后调用 `anomaly.check_*()`

**告警通道**(已有 `send_alert()`,只需加场景):
- 钉钉/企微 webhook(已支持)
- 新增:本地通知(macOS `osascript -e 'display notification'`,Linux `notify-send`)
- 新增:本地邮件(直接 SMTP)

**验收标准**:
1. 模拟 1 周日报数据入库 → 跑 `watch --dry-run` 输出所有命中规则
2. 钉钉 webhook 配置正确时 → 真告警可达
3. macOS 本地通知可达(开发机验证)
4. 测试 `tests/test_anomaly.py`(各规则单元测试)

**代码量预估**: 400~600 行

---

### #11 · 订阅与个性化(`ai-daily subscribe`)

**目标**: 用户配置"我只关心 OpenAI / Anthropic / 融资栏目",每天生成定制版。

**现状**:
- 每天一份全量 PDF,无法按兴趣筛选

**落地后**:
```bash
python main.py subscribe --add "OpenAI,Anthropic" --categories funding,policy
python main.py subscribe --list
python main.py subscribe --remove "OpenAI"
# 订阅生效:在 data/subscriptions/ 下生成定制日报(同名但加 _personalized 后缀)
python run_daily.py --full --personalized "default_user"   # 默认订阅
```

**关键模块/文件**:
- 新增 `app/personalization/filter.py`(~250 行):基于订阅规则过滤 daily JSON
- 新增 `app/personalization/store.py`(~100 行):订阅配置存 `data/subscriptions/*.yaml`
- 新增 `templates/report_personalized.html`(可选,带"为你定制"水印)

**验收标准**:
1. `subscribe --add "OpenAI"` → `data/subscriptions/default.yaml` 写入
2. 跑端到端时 `--personalized default` → 日报 PDF 仅含 OpenAI 相关条目 + 订阅栏目
3. 模板可定制:用户偏好布局
4. 测试 `tests/test_personalization.py`

**代码量预估**: 400~600 行

---

### #12 · 优雅降级与依赖预检(`ai-daily doctor --strict`)

**目标**: LLM 临时不可用 / playwright 缺失 / 额度耗尽时,优雅降级而不直接崩。

**现状**:
- LLM 限流 → 主流程崩
- playwright 未装 → 第 4 步 PDF 阶段崩(JSON 已生成但无法出报)
- 额度耗尽 → 中途崩,无提示

**落地后**:
- **依赖预检**: 跑前 `doctor` 自检(已在 #3 落地)
- **LLM 降级**:
  - LLM 不可用 → 用模板生成(更短,但能出报)
  - LLM 部分失败(50% 条目成功) → 成功的入选,失败的标占位 + `quality_flags` 告警
- **PDF 降级**:
  - playwright 未装 → 自动尝试 `weasyprint`(可选依赖)/ 仅输出 Markdown + HTML
  - PDF 失败 → 用户仍可访问 `report_{date}.html`
- **额度预警**:
  - AnySearch/Tavily 报 429 → 自动降级到摘要(不抓全文)
  - LLM 报 429 → 启动降级路径(模板生成)

**关键模块/文件**:
- 新增 `app/utils/fallback.py`(~250 行):降级路径(LLM 模板、PDF 替代)
- 新增 `app/utils/ratelimit.py`(~150 行):429/重试用量检测
- 集成进 `run_daily.py` 的各步骤,失败时尝试降级

**验收标准**:
1. 模拟 LLM 不可用(临时改 API key 失效)→ 仍产出 PDF,quality_flags 标记 LLM 失败
2. 模拟 playwright 未装 → 自动 fallback 到 Markdown + HTML,terminal 提示安装
3. 模拟 429 → 自动等待 60s 重试,3 次后降级
4. 测试 `tests/test_fallback.py` + `tests/test_ratelimit.py`

**代码量预估**: 500~700 行

---

## 三、codex 长任务拆分建议(每个建议一个 task)

| Task | 主题 | 优先级 | 依赖 | 预估代码 | 落地周期 |
|------|------|--------|------|---------|---------|
| T-A1 | 本地 Web 看板 | P0 | 无 | 800~1200 | 3~5 天 |
| T-A2 | 今日/最新快捷命令 | P0 | 无 | 150~250 | 0.5 天 |
| T-A3 | 健康检查 `doctor` | P0 | 无 | 300~400 | 1 天 |
| T-B4 | 多格式输出 | P1 | 无 | 700~900 | 2~3 天 |
| T-B5 | 衍生卡片图 | P2 | 无 | 400~500 | 1~2 天 |
| T-B6 | 本地 RSS Feed | P1 | T-A1(可共用 server) | 200~300 | 1 天 |
| T-C7 | SQLite 索引 | P0(地基) | 无 | 700~900 | 2~3 天 |
| T-C8 | 跨期对比与趋势 | P1 | T-C7 | 400~500 | 1~2 天 |
| T-C9 | 周报自动聚合 | P1 | T-C7 | 500~700 | 2 天 |
| T-D10 | 异常告警增强 | P1 | T-C7 | 400~600 | 1~2 天 |
| T-D11 | 订阅与个性化 | P2 | T-C7 + T-A1 | 400~600 | 2 天 |
| T-D12 | 优雅降级 | P0 | 无 | 500~700 | 2 天 |

**推荐 codex 任务拆分原则**:
1. 一个 task 一个独立 PR
2. 每个 task 都必须:
   - 引入新 CLI 入口(便于测试与单独使用)
   - 不破坏现有 123 个测试
   - 自带 ≥5 个新增单元测试
   - 文档(`使用实操文档.md`)同步更新
3. T-C7 是其他智能功能的地基,**强烈建议优先做**

---

## 四、可选锦上添花(用户友好度 ++,但不紧急)

| # | 主题 | 用户价值 | 实现难度 |
|---|------|---------|---------|
| O-1 | 启动画面 ASCII Art | 美观 | 0.1 天 |
| O-2 | `ai-daily config init` 交互式生成 `.env` | 零基础可用 | 0.5 天 |
| O-3 | 报告完成后自动 `osascript -e 'display notification'` | 即时反馈 | 0.2 天 |
| O-4 | 终端彩色 + 进度条(`rich`) | 视觉友好 | 0.5 天 |
| O-5 | `--dry-run` 模式:不调用任何 API,只跑通流程图 | 教学/调试 | 1 天 |
| O-6 | `ai-daily diff <date1> <date2>` 跨日报 diff | 内容审查 | 0.5 天 |
| O-7 | 自动归档:90 天前的 PDF 自动移到 `data/archive/` | 卫生 | 0.3 天 |
| O-8 | `--profile` 模式:可视化每阶段耗时(类似 cProfile) | 性能调优 | 0.5 天 |

---

## 五、迭代节奏建议

**第 1 批**(地基 + 友好性):
- T-A3 健康检查
- T-A2 快捷命令
- T-D12 优雅降级
- T-C7 SQLite 索引

**第 2 批**(看板 + 多形态):
- T-A1 本地 Web 看板
- T-B4 多格式输出
- T-B6 RSS Feed

**第 3 批**(智能 + 沉淀):
- T-C8 趋势对比
- T-C9 周报
- T-D10 异常告警

**第 4 批**(个性化):
- T-B5 衍生卡片
- T-D11 订阅
- O-1~O-8 锦上添花

---

## 六、风险与对策

| 风险 | 触发场景 | 对策 |
|------|---------|------|
| 引入新依赖导致安装失败 | starlette、playwright、pillow | 全部用 Python 标准库实现 + 标记可选依赖 |
| Web 进程与 cron 抢资源 | cron 跑日报时 web 也在跑 | web 读文件为主,无状态;cron 期间 web 可只读不触发 |
| SQLite 索引与 daily JSON 不一致 | 某次日报生成失败但 partial 写入 | DB 写入加事务,失败回滚 |
| 多格式输出阻塞主链路 | 公众号版生成慢 | 各 exporter 异步并发,失败标记不阻塞 PDF 主流程 |
| 衍生卡片图依赖中文字体 | Linux 容器环境无 Noto CJK | 模板字体加载失败时降级 ASCII |

---

## 七、验证标准(贯穿所有 task)

每个 task 提交时,必须附带:
- [ ] ≥5 个新单元测试,且全部通过
- [ ] `python3 -m pytest -q` 全绿(123 + N 个)
- [ ] `python3 run_daily.py --test` 跑通(继承 #27 入口冒烟)
- [ ] `使用实操文档.md` 同步更新新增命令
- [ ] `架构评审与优化计划.md` 文末追加落地批次(格式延续第 1~9 批)
- [ ] 新增截图或 demo 路径(用于 #1 web 看板、#5 卡片图)

---

## 八、给 codex 的执行 checklist(模板)

```
Task T-XXX:
  目标: <一句话>
  涉及文件: <列出>
  实现要点:
    - <关键点 1>
    - <关键点 2>
  验收:
    - [ ] <测试 1>
    - [ ] <测试 2>
  文档:
    - [ ] 使用实操文档.md
    - [ ] 架构评审与优化计划.md(追加批次记录)
```

---

**总评**: 前 9 批把系统从"能跑"推到"跑得稳",本批建议把系统从"跑得稳"推到"用户爱用"。单体边界完全够用,无需任何架构拆分。落地后用户将获得:**打开浏览器看历史 + 一行命令打开今天报告 + 多形态输出 + 本地情报库 + 主动告警**——一个完整的"私人 AI 财经秘书"。
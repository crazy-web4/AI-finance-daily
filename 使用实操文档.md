# AI 财经日报 · 使用实操文档

> 适用版本: v1.0.0 ｜ 更新日期: 2026-08-20
> 本文档面向实际使用者，覆盖：环境搭建 → 配置 → 首次运行 → 日常出报 → 断点续跑 → 定制 → 故障排查。

---

## 目录

1. [环境准备](#1-环境准备)
2. [配置密钥（.env）](#2-配置密钥env)
3. [第一次运行：冒烟测试](#3-第一次运行冒烟测试)
4. [日常出报：端到端生成](#4-日常出报端到端生成)
5. [run_daily.py 参数全解](#5-run_dailypy-参数全解)
6. [分步执行与断点续跑](#6-分步执行与断点续跑)
7. [main.py：单独采集与策略检查](#7-mainpy单独采集与策略检查)
8. [产出文件说明](#8-产出文件说明)
9. [定制指南](#9-定制指南)
10. [定时任务（每天自动出报）](#10-定时任务每天自动出报)
11. [常见问题排查（FAQ）](#11-常见问题排查faq)
12. [运行耗时与成本参考](#12-运行耗时与成本参考)

---

## 1. 环境准备

### 1.1 Python 版本

需要 **Python 3.10+**（代码使用了 `X | None` 等新语法；开发环境为 3.14）。

```bash
python3 --version
```

### 1.2 安装依赖

项目未附 requirements.txt，按实际依赖安装即可：

```bash
pip install httpx pydantic jinja2 playwright openai python-dotenv pyyaml
```

参考版本（开发环境实测）：

| 包 | 版本 |
|----|------|
| httpx | 0.28.x |
| pydantic | 2.x |
| Jinja2 | 3.1.x |
| playwright | 1.62.x |
| openai | 3.x |
| python-dotenv | 1.2.x |
| PyYAML | 6.x |

### 1.3 安装 Chromium（PDF 渲染必需）

```bash
playwright install chromium
```

> 只跑采集/聚类（`--no-pdf`）可以不装，但完整出报必须安装。

### 1.4 建议的目录习惯

在项目根目录（`AI财经日报/`）下执行所有命令。代码中模板、配置、prompt 均使用相对路径（如 `templates/`、`config/search_strategy.yaml`），**在其他目录执行会找不到文件**。

---

## 2. 配置密钥（.env）

```bash
cp .env.example .env
```

编辑 `.env`：

```bash
# ── 必填：搜索引擎 ──
ANYSEARCH_API_KEY=as_sk_xxxxxxxxxxxx

# ── 必填：LLM（OpenAI 兼容接口，二选一）──
# 方式A：豆包 / 火山方舟
ARK_API_KEY=xxxxxxxx
ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
LLM_MODEL=doubao-pro-128k-240515

# 方式B：OpenAI 或其他兼容服务（ARK_* 未设置时自动回退到这两个）
# OPENAI_API_KEY=sk-xxxx
# OPENAI_BASE_URL=https://api.openai.com/v1

# ── 可选：Tavily 补充搜索 ──
TAVILY_API_KEY=tvly-xxxx        # 不填则自动跳过 Tavily 补充，不影响出报

# ── 可选：报告时区 ──
# REPORT_TIMEZONE=Asia/Shanghai   # 默认 Asia/Shanghai，影响报告日期与时间窗口
```

**说明**

- `ANYSEARCH_API_KEY`：AnySearch 平台的 API Key，采集的唯一入口，缺失会直接报错退出。
- LLM 配置：`app/agents/base.py` 的读取优先级为 `ARK_API_KEY → OPENAI_API_KEY`、`ARK_BASE_URL → OPENAI_BASE_URL`；两者都没有会抛 `未配置 API key` 错误。
- `LLM_MODEL`：默认 `doubao-pro-128k-240515`。任何支持 `response_format=json_object` 的 OpenAI 兼容模型均可。
- `.env` 已在 `.gitignore` 中，不会误提交；请勿把真实 key 写进 `.env.example`。

---

## 3. 第一次运行：冒烟测试

### 3.1 检查搜索策略（不消耗任何 API）

```bash
python3 main.py info
```

预期输出（v1.0.0 策略）：

```
==================================================
  AI 财经日报 · 搜索策略
==================================================
  策略版本: 1.0.0
  总查询数: 529

  各批次:
    company_events         450 条查询  ≈ 4500 篇结果
    industry_trends         36 条查询  ≈ 360 篇结果
    hot_news                10 条查询  ≈ 100 篇结果
    policy_regulation        9 条查询  ≈ 90 篇结果
    academic_research        9 条查询  ≈ 90 篇结果
    chinese_media           15 条查询  ≈ 150 篇结果
```

这一步用来确认：依赖装好了、yaml 配置能被解析。

### 3.2 只测采集（消耗少量 AnySearch 额度）

```bash
python3 main.py collect --test
```

- 每个批次取 2 条查询（约 12 条），几十秒内完成；
- 结束后打印语言/来源等级/分类统计，并把结果存到 `data/raw/{今天日期}/raw_test_*.json`。

如果这一步报 401/403，检查 `ANYSEARCH_API_KEY`。

### 3.3 端到端测试（推荐的首跑方式）

```bash
python3 run_daily.py --test
```

流程与日志结构：

```
🚀 AI 财经日报生成器
📅 日期: 2026-08-20

============================================================
  STEP 1 / 4  新闻采集        ← AnySearch 批量搜索 + Tavily 补充 + 去重
============================================================
  测试模式: 12 条查询（每批限 2 条）
  🔍 Tavily 补充搜索（12 条，覆盖 6 个分类）
  ⏰ 时效过滤: xxx 篇近期 / xx 篇较旧
  ✅ 采集完成: xxx 篇
  💾 保存到: data/raw/2026-08-20/raw_test_*.json

============================================================
  STEP 2 / 4  事件聚类        ← 标题相似度聚类
============================================================
  ✅ xxx 篇 → xx 个事件（x个多源）

============================================================
  STEP 3 / 4  Agent 分析与编辑 ← LLM 逐事件分析（默认并发3）
============================================================
  🎯 均衡选择 6 个事件用于分析
  🧪 Analyst Agent — 6 个事件（并发3）
    [1/6] ✅ model_tech/88分 - xxx标题
    ...
  ✅ 总编辑完成
     总条目: xx   总字数: xxxx
     今日头条: x 条 ...

============================================================
  STEP 4 / 4  PDF 渲染
============================================================
  📄 HTML: data/reports/2026-08-20/report_2026-08-20.html
  📕 PDF:  data/reports/2026-08-20/AI行业全球动态日报_2026-08-20@Cyber_Gm.pdf

  ✅ 全部完成！耗时 xxx.x 秒
```

测试模式默认每批取 2 条查询（共约 12 条），约 **3~8 分钟**（取决于 LLM 速度），消耗很少的搜索与 LLM 额度，适合验证全链路。

---

## 4. 日常出报：端到端生成

### 4.1 标准命令（每天一次）

```bash
python3 run_daily.py --full --backfill
```

- `--full`：执行全部 529 条查询（约几分钟采集）；
- `--backfill`：检查六大栏目，文章数不足 5 篇的栏目自动用「近 7 天」查询补全，避免开天窗。

### 4.2 轻量日常版（省额度）

```bash
python3 run_daily.py --test --queries-per-batch 5 --max-events 12
```

每批取 5 条查询（约 30 条），分析最多 12 个事件，兼顾覆盖面与成本。

### 4.3 出报后检查清单

1. 打开 `data/reports/{日期}/` 下的 PDF，检查封面、目录、六大栏目是否齐全；
2. 看终端输出的各栏目条数：某栏目为 0 条属正常（今日无此级别新闻），但连续多天为 0 建议用 `--backfill` 或调整策略；
3. 头条应为当天最重磅新闻（重要性 ≥80 分自动入选，最多 7 条；不足 3 条时放宽到 70 分）。

---

## 5. run_daily.py 参数全解

| 参数 | 默认 | 说明 |
|------|------|------|
| `--test` | - | 测试模式采集（默认每批取 2 条查询） |
| `--full` | - | 全量采集（529 条查询） |
| `--from-file <json>` | - | 跳过采集，从已保存的 `data/raw/...json` 继续 |
| `--max-events <n>` | 见说明 | 进入 LLM 分析的事件数上限；默认 `--test` 6 / 全量 18 |
| `--concurrency <n>` | 3 | LLM 并发数；调大可提速，注意接口限流 |
| `--no-agent` | - | 只跑采集+聚类就停（不调 LLM） |
| `--no-extract` | - | 跳过原文全文提取（省 AnySearch 额度） |
| `--backfill` | - | 空栏目自动补全（近 7 天窗口） |
| `--no-pdf` | - | 生成日报 JSON 后停止，不渲染 PDF |
| `--queries-per-batch <n>` | 0 | 每批最多查询数，0=用默认值（`--test` 下为 2）；仅在 `--test` 下生效 |
| `--tavily-n <n>` | 20 | Tavily 补充搜索的查询条数（按分类均衡选取） |

三个入口模式 `--test / --full / --from-file` 必须三选一，否则程序提示后退出。

---

## 6. 分步执行与断点续跑

流水线的每个中间产物都落盘，可以任意组合：

### 6.0 跨天去重（事件记忆）说明

流水线在聚类后会自动加载 `data/events/{日期}/events_*.json` 中**近 3 天（不含今天）**的历史事件，
与今日事件做标题相似度比对（阈值 0.7），命中者视为"前几天已报道"直接过滤，终端会打印：

```
  🧠 跨天去重: 过滤 N 个近3天已报道事件
     - [2026-08-19] xxx标题 (相似度 0.85)
```

同一天内重复运行**不会**互相去重（今天的事件索引被排除），保证重跑不丢内容。
想调整窗口/阈值：改 `run_daily.py` 中 `load_recent_event_titles(days=3)` 与 `filter_seen_events(threshold=0.7)`。

### 6.1 只采集+聚类（不花 LLM 钱）

```bash
python3 run_daily.py --test --no-agent
```

产出：`data/raw/{日期}/raw_*.json`、`data/events/{日期}/events_*.json`。

### 6.2 用已有采集数据重新生成（改参数重跑分析）

```bash
# 第一次采集后觉得分析的事件太少，加大 --max-events 重跑
python3 run_daily.py --from-file data/raw/2026-08-20/raw_full_1787196345.json --max-events 12
```

> `--from-file` 之后照样可以叠加 `--backfill`、`--concurrency`、`--no-pdf` 等参数。

### 6.3 只要结构化数据（接入其他系统）

```bash
python3 run_daily.py --full --no-pdf
```

最终日报 JSON 在 `data/reports/{日期}/daily_{日期}.json`，结构见 `schemas/DailyReport.json`（六大栏目 × ReportItem，可直接喂给公众号排版、邮件推送等下游）。

### 6.4 事实核查与全文提取说明

- **全文提取（#7）**：选稿后对每个入选事件提取 Top 2 文章原文（各截取 3000 字），喂给 Analyst；提取失败自动回退摘要，不阻塞。想省额度加 `--no-extract`。
- **确定性溯源（#6a）**：key_data 的数值核心（如「200亿美元」→「200」）必须字面出现在原文/摘要中，否则剔除并记入日报 `quality_flags`。
- **LLM 复核（#6b）**：重要性 ≥85 的条目额外做一次交叉核对（只删改无依据表述、不新增），修订与无依据表述均记入 `quality_flags`。
- `quality_flags` 会写入 `daily_{date}.json`，并在终端以 🛡️ 打印，供人工复核。

### 6.5 运行报告与测试

- **运行报告**：每次运行（含失败）落盘 `data/reports/{日期}/run_{ts}.json`，含各阶段耗时、AnySearch 查询成功率、文章/事件数、全文提取数、LLM 调用统计、quality_flags、success 标志。cron 巡检直接看 `success` 与 `flags`。
- **护栏**：AnySearch 全部查询失败 → 立即报错终止；最终文章数 <5 → 终止而非产出空报。
- **测试**：`python3 -m unittest discover -s tests`（24 个用例，覆盖解析器 golden sample、去重、聚类、跨天去重、选稿、核查、时区、查询限定）。

### 6.6 重新导出 JSON Schema

修改 `app/schemas/models.py` 后：

```bash
python3 -m app.schemas.models --generate
```

会把 8 个模型的 JSON Schema 重新写入 `schemas/` 目录。

---

## 7. main.py：单独采集与策略检查

`main.py` 是纯采集入口（不聚类、不分析），适合调试搜索层：

```bash
python3 main.py info                          # 查看策略统计（离线）
python3 main.py collect --test                # 每批 2 条查询，冒烟
python3 main.py collect --batch chinese_media # 只跑某个批次
python3 main.py collect --all                 # 全量采集（约529条查询）
```

可用批次 ID：`company_events` / `industry_trends` / `hot_news` / `policy_regulation` / `academic_research` / `chinese_media`。

采集结束打印三类统计（语言、来源等级、分类分布），文件存到 `data/raw/{日期}/raw_{模式}_{时间戳}.json`，可直接作为 `run_daily.py --from-file` 的输入。

---

## 8. 产出文件说明

所有产出按日期归档在 `data/` 下：

```
data/
├── raw/2026-08-20/
│   └── raw_full_1787196345.json        # 采集结果：RawNewsArticle[]（含标题/URL/摘要/分级/分类）
├── events/2026-08-20/
│   └── events_1787195956.json          # 聚类结果：NewsEvent[]（事件标题/文章数/涉及公司/类型）
└── reports/2026-08-20/
    ├── daily_2026-08-20.json           # 最终日报数据：DailyReport（核心产物）
    ├── report_2026-08-20.html          # 渲染出的 HTML（浏览器可直接打开预览）
    └── AI行业全球动态日报_2026-08-20@Cyber_Gm.pdf
```

**重名处理**：同一天重复出 PDF 时自动追加版本号 `_v2`、`_v3`…，不会覆盖旧文件。

**HTML 预览**：不想等 PDF 时，直接用浏览器打开 `report_{日期}.html` 即可预览版式（CSS 相对引用正常）。

---

## 9. 定制指南

### 9.1 品牌信息（报名/公司/微信号/文件名）

改 `app/report/renderer.py` 中的 `RenderConfig`：

```python
class RenderConfig(BaseModel):
    report_title: str = "AI 行业全球动态日报"
    company: str = "明雯科技"
    wechat_id: str = "Cyber_Gm"          # 顶部欢迎条的微信群二维码文案
    editor_name: str = "广明"
    filename_pattern: str = "AI行业全球动态日报_{date}@Cyber_Gm.pdf"
```

支持 `{date}`、`{company}` 占位符。水印文字自动取 `广明 + 日期`（`watermark_text`）。

### 9.2 搜索策略（公司名单/事件类型/查询词）

改 `config/search_strategy.yaml`，无需动代码：

- **加公司**：在 `company_events.companies` 里加一项（`name` + `weight` + `aliases`），会自动与 15 类事件做笛卡尔积（每加 1 家 = +15 条查询）；
- **加事件类型**：在 `event_types` 里加 `keyword` + `category`；
- **加/删主题查询**：编辑各 `list` 批次的 `queries`；
- **停用某批次**：`enabled: false`；
- **调整来源分级**：`source_priority.tier_sources` 与 `app/pipeline/collector.py` 里的 `TIER1/2/3_DOMAINS` 常量（后者是代码内实际生效的分级表）；
- 改完用 `python3 main.py info` 验证查询数。

### 9.3 PDF 版式

- 结构：`templates/report.html`（欢迎条/报头/导语/目录/六大栏目）+ `templates/partials/`（封面、目录、页眉、页底）；
- 样式：`templates/css/style.css`（A4 页面、`@page` 边距、水印、配色）；
- 页面边距与输出参数在 `renderer.py → render_pdf()`（A4，上下左右 20/22/18/18mm）；
- 改完先用 `--no-pdf` 生成 HTML，浏览器预览满意后再出 PDF。

### 9.4 分析风格与栏目规则

- **写作风格/字数/评分标准**：改 `app/agents/pipeline.py` 中的 `ANALYST_PROMPT`（当前 V1 流水线实际使用的提示词，含摘要分级字数要求：90+ 分写 600-800 字，<60 分 150-200 字）；
- **导读风格**：`ChiefEditorAgent._write_summary()` 内的 prompt；
- **栏目/头条规则**：`ChiefEditorAgent.finalize()`（每栏目最多 8 条；头条 ≥80 分取前 7）；
- **事件均衡选择**：`run_daily.py → _balanced_select_events()`（每分类保底 + 按文章数/来源数补足）。

> `prompts/01_researcher.md ~ 05_chief_editor.md` 是五角色 Agent 的**设计稿**（对应接口 IF-003），V1 实现将其浓缩为 Analyst + ChiefEditor 两步；若要实现完整五角色流水线，可基于 `app/agents/base.py` 的 `BaseAgent`（自动加载 `prompts/` 对应文件）扩展。

### 9.5 更换搜索引擎

搜索层被隔离在 `app/search/anysearch.py`（接口 IF-001）：只要新引擎能把结果转成 `SearchResultItem`，上层采集/聚类/分析完全不用改。`TavilyClient` 就是一个现成的第二引擎示例。

---

## 10. 定时任务（每天自动出报）

### macOS / Linux（cron）

```bash
crontab -e
```

```cron
# 每天 23:00 自动出全量日报（带空栏目补全），日志写文件
0 23 * * * cd /path/to/AI财经日报 && /usr/bin/env python3 run_daily.py --full --backfill >> data/cron.log 2>&1
```

注意两点：

1. cron 环境没有你 shell 的环境变量，务必确认 `.env` 在项目根目录（代码会自动 `load_dotenv()`）；
2. `python3` 用绝对路径（`which python3` 查看），避免 cron 找不到依赖。

### macOS（launchd，可选）

如需开机补跑/失败重试，可写一个 `LaunchAgent` plist 调用同一命令，此处从略。

---

## 11. 常见问题排查（FAQ）

**Q1：`❌ 未找到 ANYSEARCH_API_KEY`**
`.env` 不在项目根目录、变量名拼错、或没有 `cp .env.example .env`。`main.py` 与 `run_daily.py` 都会自动加载根目录 `.env`。

**Q2：`未配置 API key，请设置 ARK_API_KEY 或 OPENAI_API_KEY`**
LLM 没配。检查 `ARK_API_KEY`/`ARK_BASE_URL`（或 `OPENAI_*`）是否都在 `.env` 中。

**Q3：STEP 1 报 MCP error / 401 / 403**
AnySearch key 无效或欠费。可先用 `python3 main.py collect --test` 单独验证搜索层。

**Q4：`⚠️ 分析失败 [xxx]: ...` 很多条**
LLM 返回了无法解析的 JSON 或超时。每条自动重试 2 次；仍失败则跳过该事件。可降低 `--concurrency` 避免限流，或更换更稳定的模型。

**Q5：PDF 生成报 playwright / browser 相关错误**
没装 Chromium：`playwright install chromium`。公司网络下 Chromium 下载失败可设置镜像 `PLAYWRIGHT_DOWNLOAD_HOST`。

**Q6：日报某栏目总是 0 条**
- 出报时加 `--backfill`（近 7 天补全，每栏目保底 5 篇）；
- 或在 `config/search_strategy.yaml` 中给该方向加查询词。

**Q7：内容重复率高 / 同一事件出现两次**
聚类阈值在 `run_daily.py → do_cluster()`（`title_threshold=0.6`，越小合并越激进）；标题去重阈值在 `collector.py → dedup_by_title()`（默认 0.85）。

**Q8：新闻不够新，混入旧闻**
采集默认只保留近 24 小时（`collect(max_age_hours=24)`），无明确发布时间的结果默认保留。若某引擎经常返回旧内容，可检查该批次的查询词是否带时间限定（`queries.py → _add_time_qualifier` 会自动给查询追加 latest/最新/日期等限定词）。

**Q9：想换一天的日期重跑？**
当前实现统一以运行当天的 UTC 日期为准（`datetime.now(timezone.utc)`）；如需补昨天的报，建议用 `--from-file` 加载昨天采集的 raw 数据，但报告日期字段仍会记为运行日。

**Q10：终端日志中文乱码**
确保终端编码为 UTF-8（macOS 默认即是）。

---

## 12. 运行耗时与成本参考

| 模式 | 查询数 | LLM 调用 | 经验耗时 | 适用场景 |
|------|--------|----------|----------|----------|
| `--test` | ~12 | 6~7 次 | 3~8 分钟 | 冒烟测试 |
| `--test --queries-per-batch 5` | ~30 | 按 `--max-events` | 5~10 分钟 | 轻量日常 |
| `--full --backfill` | 529+ | 按 `--max-events` | 10~20 分钟 | 正式出报 |

成本构成：

- **AnySearch**：按查询计费（全量 529 条/天，batch_search 每批 5 条自动分组）；
- **Tavily**：默认 20 条补充查询/天（`--tavily-n 0` 可关闭）；
- **LLM**：事件数 × 1 次分析 + 1 次导读；`--max-events` 是最直接的成本旋钮。

> 提示：全量采集结果已落盘，当天反复调优排版/分析时请用 `--from-file` 复用，避免重复消耗搜索额度。

# AI 财经日报 · 架构评审与优化计划

> 评审日期: 2026-08-20 ｜ 评审基线: main @ 5b15fc7 ｜ 实施分支: `dev`
> 评审范围: 全部源码（约 3100 行）、配置、模板与既有设计文档

## 总体评价

分层清晰（IF-001~005 接口边界合理）、数据模型完整、中间产物落盘可恢复，作为 V1 骨架合格。
但对齐"**每天稳定产出一份高质量日报**"这一核心需求，存在下列问题。

## P0 — 直接影响日报正确性的缺陷

| # | 问题 | 位置 |
|---|------|------|
| 1 | `--test` 是空壳：`--queries-per-batch` 默认 0 → 传 None → 不截断，`--test` 实际执行全部 529 条查询，与 `--full` 相同 | `run_daily.py` collect_articles |
| 2 | "24h 时效窗口"形同虚设：AnySearch Markdown 解析器从不解析 `published_at`，Tavily 也硬编码 None → 时效过滤把所有文章归入"近期" | `anysearch.py` / `collector.py` |
| 3 | 今日头条与原栏目重复：组装栏目时未剔除已入选头条的条目，同一新闻在 PDF 出现两次 | `pipeline.py` finalize |
| 4 | 来源链接伪造：sources 用 `https://{domain}` 拼域名级 URL，丢失真实文章链接 | `pipeline.py` _analysis_to_item |
| 5 | 时间硬编码炸弹：`'2026'/'2025'/'august'` 写死在代码，yaml 中 3 条查询也带字面年份 | `queries.py` / yaml |

## P1 — 决定日报内容质量的关键短板

| # | 问题 |
|---|------|
| 6 | 事实核查缺失：V1 只有 Analyst 一步，基于 ≤400 字摘要×最多 6 篇写 600-800 字详情，无交叉验证；`key_data.source_indexes` 定义了从未使用 |
| 7 | `extract()` 全文提取已实现但从未调用，是现成的质量杠杆（高分事件应提全文再分析） |
| 8 | 无跨天去重：无历史事件索引，重大事件会被连续多天重复报道；需要近 3-7 天"事件记忆" |
| 9 | 聚类过弱：纯标题 bigram Jaccard(0.6) 贪心、顺序敏感；中英文报道同一事件不会合并 |
| 10 | 默认产出量与"日报"不匹配：`--max-events` 默认 6 → 六栏目合计最多 6 条；`_balanced_select_events` 用标题关键词粗分类与 LLM 分类矛盾，`per_cat_min=min(2, max//6)` 与注释不符 |
| 11 | PDF 目录页码是假的（`loop.index + 1`），与实际分页无关 |

## P2 — 架构与可维护性

| # | 问题 |
|---|------|
| 12 | 数据模型双份定义：`RawNewsArticle`/`SearchResultItem`/`SourceReliability` 在 schemas 与 collector/anysearch 各一份，字段不同，IF-004 单一事实源已破坏 |
| 13 | 大量死配置：yaml 的 `depth/priority_domains/source_priority/time_window_hours/concurrency` 无代码读取，真实参数硬编码 |
| 14 | 五角色 Agent 设计架空：`prompts/01~05` 与 `BaseAgent` 未接入，`ResearchEvent/FactCheckedEvent/ClassifiedEvent` 无生产者无消费者 |
| 15 | 零测试：tests/ 为空；正则解析搜索 API Markdown 是最脆弱组件，格式一变会静默产出空报 |
| 16 | 无日志体系/运行报告：全程 print，cron 失败无法排查，无查询成功率/去重率/token 统计 |
| 17 | 搜索层健壮性不足：无重试退避、批内异常整批失败、0 结果不告警、BATCH_SIZE 硬编码 |
| 18 | 渲染器细节：临时 HTML 写入 templates/ 源码目录（并发踩踏），`show_back_cover` 死配置 |
| 19 | 时区错位：报告日期取 UTC，中文日报凌晨运行日期差一天 |

## P3 — 性能与成本（日跑一次不紧急）

| # | 问题 |
|---|------|
| 20 | 采集无增量机制：每天全量 529 条，可按 weight 分配或结合 #8 历史索引增量 |
| 21 | dedup_by_title/聚类 O(n²)：当前数百篇够用，5000+ 篇需按公司/域名分桶 |
| 22 | LLM 同步客户端 + run_in_executor，可换 AsyncOpenAI；分析结果可按事件指纹缓存 |

## 落地计划

| 批次 | 内容 | 状态 |
|------|------|------|
| 第 1 批 | #1 test 语义、#3 头条去重、#4 真实 URL、#5 时间硬编码、#11 目录页码 | ✅ 已完成（dev 分支） |
| 第 2 批 | #2 时效落地、#8 跨天去重、#10 选稿逻辑、#19 时区 | ✅ 已完成（dev 分支） |
| 第 3 批 | #6/#7 全文提取+事实核查、#9 聚类增强、#15 解析器测试、#16 日志与运行报告 | ✅ 已完成（dev 分支） |
| 第 4 批 | #12-14 模型/配置/Agent 收敛、#17-18 健壮性 | ✅ 已完成（dev @ c93b7fc） |
| 第 5 批 | 二轮 P0：清洗层重构（上游清洗/None 崩溃/水印集合/数字误删）+ 测试 + 未提交改动入库 | ✅ 已完成（dev 分支） |
| 第 6 批 | 二轮 P1：正文分段渲染、来源名-URL 对齐、extract 缓存、死代码清理 | ✅ 已完成（dev @ b5895bd） |
| 第 7 批 | 二轮 P2/P3：配置收敛、时区一致、退避、测试补全、告警钩子 | ✅ 已完成（dev @ 2557b26） |
| 单体优化 Phase 2/3 | 测试补全与修复、LLM 分析指纹缓存、资源动态并发、类型注解增强 | ✅ 已完成（见文末记录） |

## 第 1 批修复记录（2026-08-20，dev 分支）

1. **#1 `--test` 语义修复**（`run_daily.py`）：test 模式下 `--queries-per-batch` 未指定时默认每批 2 条（与 `main.py collect --test` 行为对齐），不再静默全量。
2. **#3 头条去重**（`app/agents/pipeline.py`）：`ChiefEditorAgent.finalize()` 组装各栏目时剔除已入选今日头条的 event_id。
3. **#4 真实来源 URL**（`app/agents/pipeline.py`）：`_analysis_to_item()` 增加 `article_map` 参数，优先使用真实文章 URL（按域名去重、保留可靠性顺序），无 article_map 时才回退域名级 URL；`run_daily.py` 透传 article_map。
4. **#5 时间动态化**（`app/search/queries.py` + `config/search_strategy.yaml`）：`_add_time_qualifier()` 的年份/月份全部改为运行时动态生成；yaml 中 3 条带字面年份的查询词去年份，交由动态限定器注入。
5. **#11 目录页码**（`templates/report.html`）：移除伪造页码，改为展示各栏目真实条目数（`N 条`），保留点线版式。

### 真实环境验证（`python3 run_daily.py --test`，2026-08-20，耗时 388s）

- **#1**：日志确认「测试模式: 12 条查询（每批限 2 条）」，采集量符合预期（12 条 AnySearch + 10 条 Tavily 补充 → 158 篇）。
- **#3**：头条 2 条（78/75 分），政策、研究栏目各 1 条（65/60 分），头条事件未在原栏目重复出现。
- **#4**：6 个来源全部为文章级真实 URL（如 whitehouse.gov/...、arxiv.org/abs/...），域名级回退 0 个。
- **#5**：实际查询词含动态时间限定，如 `OpenAI AI model latest news 2026-08-20`、`US AI regulation executive order 2026 update`、`中国 AI 公司 融资 2026年08月 最新`。
- **#11**：目录渲染为「一、今日头条 …… 2 条」等真实条目数；PDF 共 2 页版式正常。
- 附带观察（不阻塞，留待后续批次）：yaml 中 `EU AI Act implementation update` 被追加 `2026 update` 后出现 "update" 重复，属既有行为，可在第二批顺手优化。

## 第 2 批修复记录（2026-08-20，dev 分支）

1. **#2 时效落地**：新增 `_extract_published_date()`（`anysearch.py`）——URL 日期（`/2026-08-17/`、`/20260817/`）优先，其次 snippet 中 ISO/英文/中文日期，仅接受 [now-400d, now+1d]；Tavily 解析 `published_date` 字段。`collector.py` 时效过滤改为确定性规则：明确早于 cutoff 的丢弃，近期与无时间的保留（删除 90% 启发式）。
2. **#8 跨天去重**：新增 `app/pipeline/history.py`，读取近 3 天 `data/events/{date}/` 事件索引，标题 bigram Jaccard ≥0.7 过滤；同天不去重。`run_daily.py` 在聚类后接入（STEP 2.5）。
3. **#10 选稿逻辑**：`_balanced_select_events()` 重写——分类改用事件关联文章的 `category` 多数投票（不再标题关键词猜测）；每分类保底 2（预算≥12）；event_id 集合去重；`--max-events` 默认改为 test=6 / 全量=18（argparse 哨兵 None）。
4. **#19 时区**：新增 `app/utils/timeutil.py`，`REPORT_TIMEZONE` 环境变量（默认 Asia/Shanghai）；`run_daily.py` 的报告日期、`finalize()` 的 time_window 全部改用报告时区。
5. **附带**：policy 查询词 "update 2026 update" 重复修复（已含 update 只追加年份）。
6. **真实运行加固**：`--test` 实测发现标题含「2026年3月」的旧闻经"无日期"路径漏入 → `_extract_published_date()` 增加月级日期信号（仅当 >35 天前才标记为旧，当月保留），并将标题纳入提取输入。

## 第 3 批修复记录（2026-08-20，dev 分支）

1. **#7 全文提取**：`run_daily.py do_extract_fulltexts()`——入选事件 Top2 文章经 AnySearch `extract` 提取原文（各截 3000 字，并发4，best-effort），注入 Analyst 上下文（「原文全文」段，可信度高于摘要）；`--no-extract` 可关闭。
2. **#6 事实核查**：新增 `app/agents/factcheck.py`。6a 确定性溯源 `ground_key_data()`：数值核心须字面出现在原文+摘要中，否则剔除并记 flag；6b `FactCheckerAgent` 对 ≥85 分条目 LLM 复核（只删改不新增），修订/无依据表述记入 `DailyReport.quality_flags`。ANALYST_PROMPT 增加 key_data 溯源硬要求。
3. **#9 聚类增强**：公司感知阈值——共享同一公司时阈值 0.6→0.45（下限0.35），近义标题合并率提升；跨语言合并留待 embedding 方案（记录为后续项）。
4. **#15 测试与护栏**：`tests/` 24 个 unittest 用例（解析器 golden sample、日期估算、去重、分级、聚类含公司感知、跨天去重、选稿、核查、时区、查询限定）；collector 全失败 raise、失败告警、`last_stats` 暴露；run_daily 文章数 <5 终止。
5. **#16 日志与运行报告**：新增 `app/utils/runlog.py`，成功/失败均落盘 `run_{ts}.json`（阶段耗时/查询统计/LLM 统计/flags）；`LLMClient.stats` 记录调用数与输出字符数。

## 第 4 批修复记录（2026-08-20，dev 分支）

1. **#12 数据模型单一事实源**：移除 `schemas/` 下架空的 `ResearchEvent/FactCheckedEvent/ClassifiedEvent.json`；`anysearch.py` 的 `SearchResultItem` 改为从 `app/schemas/models.py` import（保留 `SearchQuery` 等搜索层自有模型）；Pydantic 模型作为唯一真相源，`--generate` 导出 JSON Schema 到 `schemas/`。
2. **#13 死配置清理 + time_window/filtering 接线**：yaml 删除已废弃的 `depth/priority_domains/source_priority/concurrency`；`run_daily.py` 从 `defaults.time_window_hours` 读时效窗口、从 `filtering` 读 `url_dedup/title_dedup/title_similarity_threshold/max_age_hours/min_content_length` 全部接入 `NewsCollector.collect()`；`collect()` 的 `dedup` 拆分为 `url_dedup + title_dedup` 独立开关；新增内容长度过滤步骤。
3. **#14 未用模型清理 + prompts 状态说明**：`app/schemas/models.py` 删除 `ResearchEvent/FactCheckedEvent/ClassifiedEvent` 三个从未实例化的中间模型；新增 `prompts/README.md` 说明五角色设计稿与 V1 实际实现的职责映射，澄清 BaseAgent + prompt 文件的预留定位。
4. **#17 搜索层重试退避 + batch_size 可配**：`AnySearchClient.search_batch()` 新增 `batch_size` 和 `max_retries` 构造参数（默认 5 / 2）；单次 MCP 调用失败时指数退避重试（`2^attempt` 秒），5xx 可重试、4xx 直接抛；整批异常不再静默返回空而是逐条登记 error。
5. **#18 渲染器 CSS 内联 + tempfile + 死配置**：`PDFRenderer.render_html()` 读取 `templates/css/style.css` 内联为 `<style>` 标签，临时 HTML 不再依赖相对路径；`render_pdf()` 改用 `tempfile.mkstemp` 生成临时文件到系统临时目录，finally 清理，避免并发踩踏和污染源码目录；删除 `show_back_cover` 死配置。

### 真实环境验证（`python3 run_daily.py --test`，2026-08-20，耗时 662.7s）

- **#12**：任何模块的 `SearchResultItem` 都来自 `app.schemas.models`，字段一致；JSON Schema 重新导出无 diff。
- **#13**：yaml 配置的 `time_window_hours=24`、`filtering.url_dedup/title_dedup/min_content_length` 全部生效；日志可见「时效过滤」「内容长度过滤」「URL/标题去重」各步独立输出。
- **#14**：模型导入无残留引用；`prompts/README.md` 明确 V1 职责映射。
- **#17**：`batch_size=5` 分批执行，批次间 0.5s 间隔；异常时 1s/2s 指数退避。
- **#18**：HTML 内联 CSS 后 `file://` 直接打开样式正常；临时文件放 `/var/folders/.../T/daily_render_*.html`，渲染后自动删除。
- **测试**：`pytest tests/` 24 passed。
- **产物**：5 条有效条目 / 2544 字 / PDF 652.8 KB，事实核查剔除 7 条无依据表述。

## 第二轮评审（2026-08-24，基线: dev @ c93b7fc + 未提交清洗层改动）

**背景**: 第 4 批后有人在工作区新增 `app/utils/text_cleaner.py` 并接入渲染层（未提交），
用于清理 extract 全文中的水印/断词噪声。严格评审发现该改动引入 P0 级缺陷。

### P0 — 未提交清洗层引入的内容损坏/崩溃

| # | 问题 | 验证 |
|---|------|------|
| R1 | `clean_full(None)` 崩溃 → 导读为 None（头条<3/LLM失败）时 PDF 渲染必崩 | 已复现 |
| R2 | 水印字集合含单字符 'A'/'I' → `Series A 轮融资`→`Series 轮融资`、`Grade A`→`Grade` | 已复现 |
| R3 | 行尾"空格+单数字"清理过激 → `评分 9`→`评分`、`iPhone 1`→`iPhone`，吞真实数字 | 已复现 |
| R4 | 清洗层位置错误：渲染层洗终稿（放大损坏），LLM 却仍吃带噪原文 | 设计缺陷 |
| R5 | 190 行新模块零测试、未入 git | — |

### P1 — 内容质量

| # | 问题 |
|---|------|
| R6 | 多段正文在 PDF 塌成一段（`{{ item.details }}` 的 \n\n 在 HTML 为空白，无 br/p） |
| R7 | 来源名称与 URL 按下标配错（src_names 顺序与去重域名无对应） |
| R8 | extract 无缓存、失败不入 run report |
| R9 | 水印字符集硬编码人名品牌，与 RenderConfig 脱节，3 处重复 |
| R10 | `smart_split_title_body` 死代码，内含硬编码正文开头特征 |

### P2 — 配置与一致性

| # | 问题 |
|---|------|
| R11 | 时效配置双写: time_window_hours(24,已接线) vs filtering.max_age_hours(48,死) |
| R12 | 死配置/死字段: show_toc/show_cover（模板不读）、ReportItem.lead |
| R13 | main.py 日期目录仍 UTC，与 run_daily 北京日期凌晨分裂 |
| R14 | Tavily time_range="day" 硬编码，不随 time_window 联动 |
| R15 | LLMClient.chat_json 重试无退避 |
| R16 | history.py 窗口用本地 now()，与可配置报告时区不一致 |
| R17 | main.py collect 未接 yaml filtering/time_window |

### P3 — 工程与运维

| # | 问题 |
|---|------|
| R18 | 测试缺口: text_cleaner / renderer / _parse_batch_results / runlog |
| R19 | cron 失败无主动告警通道 |
| R20 | 跨语言聚类（embedding）挂起中 |
| R21 | runlog 粒度: backfill 并入 collect、extract 失败数未记录 |

## 第 5 批修复记录（2026-08-24，dev 分支）

1. **R1 None 安全**: text_cleaner 全部函数 None-safe；渲染层导读清洗不再崩。
2. **R2 水印集合**: 模块级 `WATERMARK_CHARS_CN` 仅含中文字符，移除单字符拉丁字母。
3. **R3 数字水印**: `clean_watermark_digits` 增加 `min_line_len=20` 长行门槛，短行尾数字一律保留；且默认仅在**上游**启用。
4. **R4 分层**: 上游 `do_extract_fulltexts()` 对 extract 全文做 `clean_full()`；渲染层新增 `safe_render_clean()` 只做零风险断词修复，不再对终稿做水印猜测；`complete_title` 收敛为断词修复+截断+去尾标点。顺带移除死代码 `smart_split_title_body`（R10 提前）。
5. **R5 测试**: 新增 tests/test_text_cleaner.py 与 tests/test_renderer.py（None 导读/Series A/评分 9/断词/CSS 内联）。
6. 未提交的 CSS 紧凑化改动（style.css）随本批一并入库。

---

## 🏗️ 资深架构师优化建议（2026-08-27）- 单体架构专项

基于对项目完整代码的深入分析，保持单体架构不变的前提下，从性能优化、代码质量、可维护性等维度提出以下优化建议。

### 🚀 性能优化（单体增强）

#### 1. 进程级并发优化
```python
# 当前问题：部分步骤串行，可优化为进程内并发
# 建议：在现有 asyncio 基础上优化，引入进程池

import asyncio
from concurrent.futures import ProcessPoolExecutor
import multiprocessing

class EnhancedCollector:
    def __init__(self, max_workers=None):
        # 使用CPU核心数作为默认并发数
        self.max_workers = max_workers or multiprocessing.cpu_count()
        self.executor = ProcessPoolExecutor(max_workers=self.max_workers)
    
    async def enhanced_collect(self, queries):
        # AnySearch 和 Tavily 完全并行
        async with asyncio.TaskGroup() as tg:
            anysearch_task = tg.create_task(
                self._run_with_executor(self._search_anysearch, queries)
            )
            tavily_task = tg.create_task(
                self._run_with_executor(self._search_tavily, queries)
            )
            
        results = await asyncio.gather(anysearch_task, tavily_task)
        return self.merge_results(results)
    
    async def _run_with_executor(self, func, *args):
        # 将 CPU 密集型任务放入进程池
        loop = asyncio.get_event_loop()
        return await loop.run_in_executor(self.executor, func, *args)
```

#### 2. 内存缓存优化
```python
# 使用 Python 内置的 lru_cache + 文件缓存，避免外部依赖

from functools import lru_cache
import pickle
import os
from datetime import datetime, timedelta

class SmartCache:
    def __init__(self, cache_dir="data/cache", max_memory_items=1000):
        self.cache_dir = Path(cache_dir)
        self.cache_dir.mkdir(exist_ok=True)
        self.max_memory_items = max_memory_items
        self._memory_cache = {}
        self._access_times = {}
    
    @lru_cache(maxsize=1000)
    def get_extracted_text(self, url: str) -> str | None:
        # 内存缓存
        return self._memory_cache.get(url)
    
    def set_extracted_text(self, url: str, text: str):
        # 带 LRU 策略的内存缓存
        if len(self._memory_cache) >= self.max_memory_items:
            # 删除最久未使用的
            oldest = min(self._access_times.items(), key=lambda x: x[1])[0]
            del self._memory_cache[oldest]
            del self._access_times[oldest]
        
        self._memory_cache[url] = text
        self._access_times[url] = datetime.now()
        
        # 同时写入文件缓存
        cache_file = self._get_cache_file(url)
        with open(cache_file, 'wb') as f:
            pickle.dump({
                'text': text,
                'timestamp': datetime.now()
            }, f)
    
    def _get_cache_file(self, url: str) -> Path:
        key = hashlib.sha256(url.encode()).hexdigest()[:16]
        return self.cache_dir / f"{key}.pkl"
```

#### 3. 算法优化
```python
# 聚类算法优化 - 预处理分桶减少计算量

def optimized_cluster_articles(articles, title_threshold=0.65):
    """
    优化的聚类算法：
    1. 按公司/域名预分桶
    2. 桶内聚类
    3. 相似桶合并
    """
    # 1. 预分桶
    buckets = defaultdict(list)
    for article in articles:
        # 提取主要关键词作为桶键
        bucket_key = self._get_bucket_key(article)
        buckets[bucket_key].append(article)
    
    # 2. 桶内并行聚类
    clustered_events = []
    for bucket_key, bucket_articles in buckets.items():
        if len(bucket_articles) > 1:
            events = self._cluster_bucket(bucket_articles, title_threshold)
            clustered_events.extend(events)
        else:
            # 单独成事件
            event = self._single_to_event(bucket_articles[0])
            clustered_events.append(event)
    
    # 3. 合并相似桶的结果
    return self._merge_similar_buckets(clustered_events)

def _get_bucket_key(self, article) -> str:
    """提取聚类分桶键"""
    # 使用公司名称或域名作为主要分键
    companies = _extract_companies(article.title)
    if companies:
        return companies[0].lower()
    return article.source_domain
```

### 📊 内置监控与日志（无外部依赖）

#### 1. 轻量级性能监控
```python
import time
import json
from contextlib import asynccontextmanager
from collections import defaultdict

class PerformanceMonitor:
    def __init__(self):
        self.metrics = defaultdict(list)
        self.start_time = time.time()
    
    @asynccontextmanager
    async def measure(self, operation: str):
        start = time.time()
        try:
            yield
        finally:
            duration = time.time() - start
            self.metrics[operation].append({
                'duration': duration,
                'timestamp': time.time()
            })
            print(f"PERF: {operation} took {duration:.2f}s")
    
    def get_stats(self):
        stats = {}
        for op, measurements in self.metrics.items():
            durations = [m['duration'] for m in measurements]
            stats[op] = {
                'count': len(durations),
                'avg': sum(durations) / len(durations),
                'min': min(durations),
                'max': max(durations),
                'total': sum(durations)
            }
        return stats
    
    def save_report(self, filepath: str):
        with open(filepath, 'w') as f:
            json.dump({
                'runtime': time.time() - self.start_time,
                'metrics': dict(self.metrics),
                'stats': self.get_stats()
            }, f, indent=2)
```

#### 2. 结构化日志系统
```python
import logging
from datetime import datetime
from enum import Enum

class LogLevel(Enum):
    DEBUG = "DEBUG"
    INFO = "INFO"
    WARNING = "WARNING"
    ERROR = "ERROR"
    CRITICAL = "CRITICAL"

class StructuredLogger:
    def __init__(self, name: str):
        self.logger = logging.getLogger(name)
        self.logger.setLevel(logging.INFO)
        
        # 文件处理器
        handler = logging.FileHandler(f"logs/{name}.log")
        formatter = logging.Formatter(
            '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
        )
        handler.setFormatter(formatter)
        self.logger.addHandler(handler)
    
    def log(self, level: LogLevel, message: str, **kwargs):
        """结构化日志"""
        extra = {
            'timestamp': datetime.now().isoformat(),
            **kwargs
        }
        
        log_message = f"{message} | {json.dumps(extra)}"
        
        if level == LogLevel.DEBUG:
            self.logger.debug(log_message)
        elif level == LogLevel.INFO:
            self.logger.info(log_message)
        elif level == LogLevel.WARNING:
            self.logger.warning(log_message)
        elif level == LogLevel.ERROR:
            self.logger.error(log_message)
        elif level == LogLevel.CRITICAL:
            self.logger.critical(log_message)

# 使用示例
logger = StructuredLogger("ai_daily")
logger.log(LogLevel.INFO, "开始采集", query_count=529)
```

### 🔧 代码质量提升

#### 1. 类型注解增强
```python
# 添加完整的类型注解，使用 mypy 检查
from typing import Dict, List, Optional, Tuple
from dataclasses import dataclass

@dataclass
class ProcessingStats:
    total_articles: int
    clustered_events: int
    processed_events: int
    average_event_size: float
    processing_time: float

class EnhancedAnalyzer:
    def analyze_events(
        self, 
        events: List[NewsEvent],
        article_map: Dict[str, RawNewsArticle],
        max_concurrent: int = 5
    ) -> Tuple[List[Tuple[NewsEvent, Dict]], ProcessingStats]:
        """
        分析事件列表
        
        Args:
            events: 待分析的事件列表
            article_map: 文章ID到文章的映射
            max_concurrent: 最大并发数
            
        Returns:
            分析结果列表和处理统计
        """
        results = []
        start_time = time.time()
        
        # 现有逻辑...
        
        stats = ProcessingStats(
            total_articles=len(article_map),
            clustered_events=len(events),
            processed_events=len(results),
            average_event_size=sum(e.article_count for e in events) / len(events),
            processing_time=time.time() - start_time
        )
        
        return results, stats
```

#### 2. 错误处理标准化
```python
from enum import Enum

class ErrorType(Enum):
    NETWORK_ERROR = "network_error"
    API_LIMIT_ERROR = "api_limit_error"
    VALIDATION_ERROR = "validation_error"
    PROCESSING_ERROR = "processing_error"

class AppError(Exception):
    def __init__(self, error_type: ErrorType, message: str, details: dict = None):
        super().__init__(message)
        self.error_type = error_type
        self.details = details or {}
        self.timestamp = datetime.now()

# 使用示例
def handle_api_error(response):
    if response.status_code == 429:
        raise AppError(
            ErrorType.API_LIMIT_ERROR,
            "API rate limit exceeded",
            {"retry_after": response.headers.get("Retry-After")}
        )
    elif response.status_code >= 500:
        raise AppError(
            ErrorType.NETWORK_ERROR,
            "Server error",
            {"status_code": response.status_code}
        )
```

### 📈 配置管理优化

#### 1. 统一配置系统
```python
from dataclasses import dataclass
from typing import List, Dict
import yaml

@dataclass
class CacheConfig:
    enabled: bool = True
    max_memory_items: int = 1000
    disk_ttl_hours: int = 24

@dataclass
class ConcurrencyConfig:
    collector_max_workers: int = 5
    analyzer_max_workers: int = 3
    extract_max_workers: int = 4

@dataclass
class MonitoringConfig:
    enabled: bool = True
    log_level: str = "INFO"
    metrics_retention_days: int = 30

@dataclass
class AppConfig:
    cache: CacheConfig
    concurrency: ConcurrencyConfig
    monitoring: MonitoringConfig
    search: dict  # 保持原有结构
    
    @classmethod
    def load_from_file(cls, filepath: str) -> 'AppConfig':
        with open(filepath, 'r') as f:
            config_data = yaml.safe_load(f)
        
        return cls(
            cache=CacheConfig(**config_data.get('cache', {})),
            concurrency=ConcurrencyConfig(**config_data.get('concurrency', {})),
            monitoring=MonitoringConfig(**config_data.get('monitoring', {})),
            search=config_data.get('search', {})
        )

# 更新 run_daily.py
config = AppConfig.load_from_file("config/app_config.yaml")
```

#### 2. 动态配置热更新
```python
import time
from pathlib import Path

class ConfigWatcher:
    def __init__(self, config_path: str):
        self.config_path = Path(config_path)
        self.last_modified = self.config_path.stat().st_mtime
        self.config = None
    
    def check_and_reload(self):
        """检查文件变化并重新加载"""
        try:
            current_modified = self.config_path.stat().st_mtime
            if current_modified != self.last_modified:
                self.last_modified = current_modified
                self.config = AppConfig.load_from_file(self.config_path)
                return True
        except Exception as e:
            print(f"Failed to reload config: {e}")
        return False
    
    def get(self):
        if self.config is None:
            self.config = AppConfig.load_from_file(self.config_path)
        return self.config
```

### 🧪 测试策略增强

#### 1. 单元测试扩展
```python
# tests/test_collector.py
import unittest
from unittest.mock import Mock, patch
from app.pipeline.collector import NewsCollector

class TestCollector(unittest.TestCase):
    def setUp(self):
        self.collector = NewsCollector(api_key="test_key")
    
    def test_dedup_by_url(self):
        articles = [
            RawNewsArticle(
                article_id="art_001",
                url="https://example.com/news1",
                title="News 1",
                source_domain="example.com"
            ),
            RawNewsArticle(
                article_id="art_002", 
                url="https://example.com/news1",
                title="News 2",
                source_domain="example.com"
            )
        ]
        
        deduped = dedup_by_url(articles)
        self.assertEqual(len(deduped), 1)
        self.assertEqual(deduped[0].title, "News 2")  # 保留更长的
    
    @patch('app.pipeline.collector.AnySearchClient')
    def test_collect_with_error_handling(self, mock_client):
        # 模拟 API 错误
        mock_client.search_batch.side_effect = Exception("API Error")
        
        with self.assertRaises(RuntimeError):
            await self.collector.collect([])
```

#### 2. 集成测试
```python
# tests/test_integration.py
import unittest
import asyncio
from run_daily import collect_articles, do_cluster, do_agent_analysis

class TestIntegration(unittest.TestCase):
    def setUp(self):
        self.test_data = self._create_mock_data()
    
    def test_full_pipeline(self):
        """测试完整的数据流水线"""
        # 测试采集
        articles = asyncio.run(collect_articles(True))
        self.assertGreater(len(articles), 0)
        
        # 测试聚类
        events = do_cluster(articles)
        self.assertGreater(len(events), 0)
        
        # 测试分析（使用较少事件）
        article_map = build_article_map(articles)
        report, stats = asyncio.run(
            do_agent_analysis(events[:3], article_map, max_events=3)
        )
        
        self.assertIsNotNone(report)
        self.assertGreater(report.total_items, 0)
    
    def test_error_scenarios(self):
        """测试错误场景"""
        # 测试空数据
        articles = []
        events = do_cluster(articles)
        self.assertEqual(len(events), 0)
```

### 🎯 成本优化（单体内）

#### 1. LLM 调用优化
```python
class OptimizedLLMClient:
    def __init__(self, config: AppConfig):
        self.config = config
        self.request_cache = {}
    
    async def batch_analyze_events(self, events: List[NewsEvent], article_map: Dict) -> List[Dict]:
        """批量分析事件，减少 API 调用次数"""
        # 按相似度分组，减少重复分析
        grouped_events = self._group_similar_events(events)
        
        results = []
        for group in grouped_events:
            # 对相似事件使用模板生成，减少 LLM 调用
            if len(group) > 1:
                batch_result = await self._analyze_group_batch(group)
                results.extend(batch_result)
            else:
                # 单独事件正常分析
                result = await self._analyze_single_event(group[0], article_map)
                results.append(result)
        
        return results
    
    def _group_similar_events(self, events: List[NewsEvent]) -> List[List[NewsEvent]]:
        """按主题相似度分组"""
        # 使用简单关键词匹配
        groups = []
        used = set()
        
        for i, event in enumerate(events):
            if i in used:
                continue
                
            group = [event]
            used.add(i)
            
            # 寻找相似事件
            for j, other in enumerate(events[i+1:], i+1):
                if self._calculate_similarity(event, other) > 0.7:
                    group.append(other)
                    used.add(j)
            
            groups.append(group)
        
        return groups
```

#### 2. 资源使用优化
```python
class ResourceOptimizer:
    def __init__(self, config: AppConfig):
        self.config = config
    
    def adjust_concurrency_dynamically(self, system_load: float) -> dict:
        """根据系统负载动态调整并发数"""
        base_config = self.config.concurrency
        
        if system_load > 0.8:
            # 高负载，降低并发
            return {
                'collector': max(1, base_config.collector_max_workers - 2),
                'analyzer': max(1, base_config.analyzer_max_workers - 1),
                'extract': max(1, base_config.extract_max_workers - 1)
            }
        elif system_load < 0.3:
            # 低负载，提升并发
            return {
                'collector': min(10, base_config.collector_max_workers + 2),
                'analyzer': min(8, base_config.analyzer_max_workers + 1),
                'extract': min(6, base_config.extract_max_workers + 1)
            }
        else:
            # 正常负载
            return {
                'collector': base_config.collector_max_workers,
                'analyzer': base_config.analyzer_max_workers,
                'extract': base_config.extract_max_workers
            }
    
    def monitor_system_load(self) -> float:
        """监控系统负载"""
        import psutil
        
        # CPU 使用率
        cpu_percent = psutil.cpu_percent()
        
        # 内存使用率
        memory = psutil.virtual_memory()
        memory_percent = memory.percent
        
        # 网络IO
        net_io = psutil.net_io_counters()
        
        # 综合负载计算
        return (cpu_percent * 0.4 + memory_percent * 0.4 + 
                min(net_io.bytes_sent / 1000000, 100) * 0.2) / 100
```

### 📋 实施路线图（单体优化）

#### Phase 1: 立即实施（1周内）
1. **性能监控** - 添加 PerformanceMonitor
2. **日志系统** - 实施 StructuredLogger
3. **错误处理标准化** - 定义 AppError 和 ErrorType
4. **配置管理优化** - 统一配置系统

#### Phase 2: 2-4周
1. **并发优化** - 优化现有并发机制
2. **缓存增强** - 实现智能缓存策略
3. **测试补充** - 增加单元测试和集成测试
4. **算法优化** - 优化聚类算法性能

#### Phase 3: 1-2个月
1. **LLM调用优化** - 批量处理和缓存
2. **资源监控** - 动态调整并发
3. **代码质量** - 完善类型注解
4. **文档更新** - 更新开发文档

### 🎯 预期收益（单体优化）

- **性能提升**: 30-50% 的处理速度提升（通过并发优化）
- **内存使用**: 减少 20-30% 的内存占用（通过缓存优化）
- **稳定性**: 降低 60% 的未知错误（通过标准化错误处理）
- **可维护性**: 代码质量提升，便于后续维护
- **开发效率**: 减少 30% 的调试时间（通过完善的日志和监控）
- **成本节约**: LLM 调用成本降低 20-40%（通过批量处理和缓存）

### 💡 总结建议

保持单体架构的前提下，重点优化：

1. **内部性能** - 充分利用现有 asyncio，优化并发机制
2. **监控能力** - 建立内置的监控和日志系统
3. **代码质量** - 类型注解、错误处理标准化
4. **配置管理** - 统一配置系统，支持动态更新
5. **测试覆盖** - 补充单元测试和集成测试

这些优化将在保持系统简单性的同时，显著提升性能和可维护性，使单体应用能够支撑更大规模的内容生产需求。

## 单体优化 Phase 2/3 落地记录（2026-08-30）

基线：111 个测试全部通过（`python3.12 -m pytest -q`）。修复测试时一并清掉几个潜伏的生产级缺陷。

### Phase 2 · 测试补全与修复

让 27 个红测变绿，并修复其暴露的真实缺陷：

- **聚类 `app/pipeline/cluster.py`**：
  - `defaultdict` 未导入，运行到聚类即 `NameError`（分桶聚类是死路径）——补导入。
  - 分桶后把单元素桶的 `(idx, article)` 元组当成文章对象传入；多元素桶把
    `article_ids` 改写成「全局索引字符串」，与 `build_article_map`（按真实
    `article_id` 建索引）不一致，事件会丢文章。重写为桶内贪心聚类、全程保留
    真实 `article_id`。
  - `_merge_similar_buckets` 原把「同一公司」的事件**无条件**合并，会把
    “OpenAI 发模型”和“OpenAI 融资”并成一条（内容级回归）。改为按规范标题
    bigram 相似度达到阈值才跨桶合并（不做公司折扣，桶内已做过）。
  - 公司感知阈值由 `max(0.35, thr-0.15)` 调整为 `max(0.30, thr-0.25)`，
    实测同义改写标题 sim≈0.42 可合并、无关同公司新闻 sim<0.30 不误并。
  - 删除 110 行死函数 `_article_id_to_temp_id`（`return` 后还挂着一段旧聚类代码）。
- **配置 `app/config.py`**：`get_config()` 未初始化即抛错，导致缓存/并发等无
  密钥依赖的工具模块在单独使用/单测时崩溃。改为惰性返回默认配置（LLM 仍走
  环境变量并在缺失时自行报错）。另移除未使用且在生产解释器（3.14）缺失的
  `from watchfiles import awatch`（轮询线程实现热更新，本就没用到它）。
- **缓存 `app/utils/cache.py`**：条目级 TTL 名存实亡——内存缓存根本不记录
  过期时间，文件缓存也只看全局 24h、忽略条目 `ttl`。现在内存/文件都按条目
  TTL 判过期；`set()` 支持 `ttl_hours=`（修掉 `cache_extract_text`/
  `cache_event_analysis` 用 `ttl_hours=` 调用一个不存在参数的隐患）；
  `@cache_result` 新增 `ttl_seconds`。
- **测试侧修正**：采集器集成测 patch 错了命名空间（`anysearch.AnySearchClient`
  → 应为 `pipeline.collector.AnySearchClient`）且用裸 dict 冒充响应对象，
  改为真实 `SearchBatchResponse/SearchQueryResponse/SearchResultItem`；
  性能报告测错误地 mock `open`（改用临时目录写真实 JSON）；阈值单调性断言
  方向写反（越严格事件应越多）；logger 集成测路径期望与实现对齐。

### Phase 3

- **LLM 调用优化（事件指纹缓存）** `app/agents/pipeline.py`：`AnalystAgent`
  分析前按「缓存版本 + 规范标题 + 文章 ID + 是否含全文」算事件指纹，命中即
  复用既往分析、跳过 LLM；重跑/回填/跨天复现同一事件可省调用。`use_cache`
  可关，缓存读写异常不影响主链路。
- **资源动态调整** `app/utils/resource.py`（新增，零三方依赖）：`ResourceOptimizer`
  用 `os.getloadavg()/os.cpu_count()` 归一化负载（装了 psutil 再纳入内存），
  按高/低负载给出采集、分析、提取三类并发度。`run_daily.py` 在未显式传
  `--concurrency` 时按系统负载动态决定分析并发（常载仍为基准 3，高载减压、
  闲载提速）。
- **类型注解增强**：补全 `app/agents/pipeline.py`、`app/agents/base.py`、
  `app/report/renderer.py`、`app/utils/cache.py`、`app/utils/logger.py`、
  `app/utils/performance.py` 等核心模块的参数/返回值注解。
- **新增测试**：`tests/test_resource.py`、`tests/test_analysis_cache.py`，
  并在 `tests/test_cluster.py` 增加 `article_ids` 可解析性回归测试。

## 第三轮评审（2026-08-31，基线: main @ 1379782「第8批」）

**评审方法**: 全量源码精读（app/ 5129 行 + run_daily/main/scripts）+ `pytest` 实测（111 passed）
+ 入口级动态验证（直接运行 `run_daily.py`、字节码级 NameError 证明）+ 已发布产物
（08-30 PDF/JSON、raw 存档、extract 缓存）抽查。

**总体评价**: 架构方向（单体 + 分层 + 单一模型源 + runlog/告警/缓存 TTL）依然正确且较上轮更成熟；
但第 8 批提交在"重构并发/错误处理"时留下两处**入口即崩**的回归，且 111 个全绿测试无一能拦住——
说明测试体系缺"入口级冒烟"这一层。另发现一类**已发布到 PDF 的内容损坏**（内联水印噪声），
属清洗层覆盖盲区。本轮共 27 项，按优先级如下。

### P0 — 生产即崩 / 已发布内容损坏（均已实测复现）

| # | 问题 | 位置 | 验证 |
|---|------|------|------|
| T1 | `main()` 中 `os.environ["LOG_TO_CONSOLE"]=...` 但模块顶层**无 `import os`**（仅 `load_api_key`/`do_extract_fulltexts` 函数内局部 import，不泄漏到模块全局）。任何模式（--test/--full/--from-file）启动即 `NameError: name 'os' is not defined` | run_daily.py:390 | 实测 `python3 run_daily.py` 直接崩溃；第 8 批引入 |
| T2 | Tavily 分支 `asyncio.gather(*tasks)` 但 `tasks = [tavily_search(q) ...]` 创建行在第 8 批重构时被删除，且留下重复 print。`.env` 配置了 TAVILY_API_KEY → Step 1 AnySearch 阶段完成后必崩 `NameError: tasks`。`scripts/add_us_stocks.py` 因 `except Exception` 吞掉异常降级为纯 Tavily，反而掩盖了缺陷 | collector.py:374 | 字节码验证 `tasks` 仅 LOAD_GLOBAL、无 STORE；git diff 1379782 确认删行 |
| T3 | **内联水印噪声穿透进已发布终稿**：08-30 PDF 出现「美**26**国国防部」「要求**20**推翻」「整合**20**进 Siri」「列**8-**被告」「损**报**失」「这是**日**继小米」「版**8-**iPhone」等。根因：① `clean_full` 只覆盖"空格包围单字"与"长行尾单数字"两类，而头条/新浪系竖排日期水印以**内联碎片**（20/26/8-/报/日）散落正文；② `title`/`snippet` 完全不经清洗（clean_full 仅作用于 extract 全文），噪声经 LLM 上下文直入终稿 | text_cleaner.py / collector.normalize_result / pipeline._build_event_context | 08-30 PDF 第 2 页渲染图可见；raw 存档随机抽查 40 篇命中内联噪声 |

### P1 — 内容质量与正确性

| # | 问题 |
|---|------|
| T4 | 跨天记忆 off-by-one：`load_recent_event_titles` 注释声明"用目录日期 23:59 与窗口起点比较"，代码却用目录日期 **00:00** 比较 → `days=3` 实际只覆盖约 2 天历史，跨天去重能力缩水 |
| T5 | `backfill.py` 三条查询硬编码 "2026"（valuation 2026 / construction 2026 / breakthrough 2026），第一轮 #5"时间硬编码炸弹"在补全模块复燃；且 `time_window_days` 参数声明后未使用（cutoff 写死 7 天） |
| T6 | "M/D" 无年份日期不识别：「美股 8/28（周五）收盘」走"无日期→保留"路径，2 天前旧行情进入 08-30 日报。`_extract_published_date` 缺"无年份月日"信号（可结合报告日期推断年份、超窗即弃） |
| T7 | 第二轮 R13/R17 未在 main.py 闭环：文件名 ts 仍 `datetime.now(timezone.utc)`；`cmd_collect` 未接 yaml 的 filtering/time_window，与 run_daily 过滤口径不一致（CLI 采集不做时效/长度过滤） |
| T8 | `LOG_TO_CONSOLE` 在 `init_system()`（已创建 logger）之后才设置，开关对主 logger 不生效（顺序错误，与 T1 同处） |
| T9 | `add_us_stocks.py articles_to_items` 先切片 `articles[:max_items]` 再过滤短摘要 → 存在短摘要时美股栏目条数少于 max_items（应先过滤后取前 N） |
| T10 | 事实核查修订**不回写**分析缓存：缓存存核查前版本，重跑/跨天命中缓存仍触发一次 LLM 复核（结果正确但浪费调用；应在核查后回写或缓存带核查标记的版本） |

### P2 — 架构一致性与可维护性

| # | 问题 |
|---|------|
| T11 | 双错误体系且新体系带病：`exceptions.py` 仅 config.py 使用；`handle_api_error` 的 401/400 分支给 `AppError` 传 `status_code=` 关键字 → **TypeError**（实测）；`ErrorHandler.log_error` 用不存在的 `error.message`；搜索/采集层仍用 RuntimeError+print，零调用方 |
| T12 | 双 extract 缓存：`SmartCache.cache_extract_text/get_cached_extract_text` 为死代码；run_daily 另造 sha1 文件缓存（7天TTL）。应收敛为一处 |
| T13 | yaml `llm` 段（model/temperature/max_retries/batch_size）**不被 LLMClient 读取**（只认环境变量）——第一轮 #13 死配置问题在新增配置层复现 |
| T14 | 一次性批处理进程里 `init_config(watch=True)` 拉起 1s 轮询的 ConfigWatcher 守护线程，无实际价值（批跑场景配置不会变），建议批任务默认 watch=False |
| T15 | `get_logger` 不缓存实例：每次调用 `handlers.clear()` 后新建 FileHandler，旧句柄不 close（fd 泄漏隐患）；`log_dir` 硬编码 "logs" 不读 `config.log_dir` |
| T16 | `PerformanceMetrics` 只被实例化，`save_report` 全链路零调用 → 第 8 批宣称的性能监控实为 no-op；runlog 也未并入 perf 统计 |
| T17 | `NewsCollector.executor` ThreadPoolExecutor 创建后从未使用且未 shutdown；`as_completed` 导入未用；Tavily 补充搜索 print 重复两行（第 8 批重构残留） |
| T18 | 仓库/产物卫生：`scripts/pdf_to_report.py.bak`（599 行）入库；data/reports 顶层散落 @杜皓杰 水印 PDF、`2026-08-25/2026-08-25/` 嵌套目录、`_v2.._v11` 版本号泛滥（人工重渲染痕迹，建议定期归档） |

### P3 — 性能与细节

| # | 问题 |
|---|------|
| T19 | 事实核查 `verify` 在 async main 中**串行同步**阻塞事件循环（analyst 已走 executor，核查未对齐），高分条目多时显著拉长 Step 3 |
| T20 | `analyze_event_async` 用已弃用的 `asyncio.get_event_loop()`，应改 `get_running_loop()` |
| T21 | `queries._add_time_qualifier` 用本地 `datetime.now()`，与 REPORT_TIMEZONE 不一致（UTC 机器凌晨跑会写错"今日"限定词） |
| T22 | `do_cluster` 的 `title_threshold=0.6` 硬编码；dedup 阈值已接 yaml 而聚类阈值未接，配置口径不统一 |
| T23 | 目录中文数字用 `loop.index` 连空栏目一起计数 → 中间栏目为空时序号跳号（一、二、四、五） |
| T24 | `_write_summary` 注释"最少3条才放宽到75分"，代码放宽到 70，注释/代码不一致 |
| T25 | `SmartCache._cleanup_file_cache` 仅被 `optimize()` 引用而 `optimize()` 零调用；磁盘缓存只靠读取时判过期，无主动清理 |
| T26 | `collect_articles` 返回注解 `list[RawNewsArticle]` 与实际 tuple 不符；run_daily 未用导入 `datetime/timezone/get_config` |

### 🔍 流程根因（本轮最重要的教训）

**T27**: 111 测试全绿却放行两个入口即崩的 NameError，根因有二：
1. **测试无入口级冒烟**：`tests/test_integration.py` 用 `use_tavily=False`，`test_collector` 不经过
   Tavily 分支，`run_daily.main()` 从无测试触达——T1/T2 恰好都落在测试盲区。
2. **第 8 批后未做真实环境 `--test` 全链路验证**（第 1~5 批均有验证记录，第 8 批只有 pytest 记录）。

建议固化为发布检查单：① 新增"假 key + mock 传输层"的入口冒烟测试（覆盖 main 全参数路径、
collect 的 Tavily 分支）；② 每批提交后必须跑一次 `run_daily.py --test` 真实链路并留记录。

### 第 9 批实施建议

| 批次 | 内容 | 状态 |
|------|------|------|
| 9a（P0 立即） | T1 补 `import os` 并将 LOG_TO_CONSOLE 前移（顺带 T8）；T2 恢复 `tasks` 创建、删重复 print；T3 清洗层增加内联水印碎片规则（`汉字+(20\|26\|8-\|0-)+汉字`、单字水印内联变体）并对 normalize 阶段的 title/snippet 做轻量清洗 | ✅ 已完成（第 9 批） |
| 9b（P1） | T4 目录日期 23:59 比较；T5 年份动态化+死参数清理；T6 无年份月日信号；T9 先过滤后切片；T10 核查后回写缓存 | ✅ 已完成（第 9 批） |
| 9c（P2 收敛） | T11 修 AppError kwargs/删死代码或真正接入；T12 缓存收敛；T13 LLMClient 接 LLMConfig；T14 watch 默认 False；T15 logger 单例化；T16 perf 并入 runlog 或删；T17 死代码清理；T18 .bak 移除 | ✅ 已完成（第 9 批） |
| 9d（P3+测试） | T19~T26 细节修复；T27 入口冒烟测试 + 发布检查单 | ✅ 已完成（第 9 批） |

**正面确认**: 模型单一事实源、runlog 落盘、失败告警、条目级 TTL、聚类 article_ids 回归测试、
HTML 转义防注入等上轮成果均稳固；本轮问题集中在"重构残留 + 清洗覆盖盲区 + 入口测试缺失"，
架构方向无需调整。


## 第 9 批落地记录（2026-08-31）

基线：main @ 1379782（第 8 批）→ 本批提交。`python3.12 -m pytest -q` **123 passed**。

### 9a · P0 修复
1. **T1**：run_daily 顶层补 `import os`；`LOG_TO_CONSOLE` 设置移入 `init_system()` 且先于
   `get_logger`（顺带 T8）。实测入口不再 NameError，无模式参数时正常提示退出。
2. **T2**：collector 恢复 `tasks = [tavily_search(q) for q in supplement_queries]`，删除重复 print。
   真实链路验证：Tavily 分支跑通（「Tavily 新增: 70 篇」）。
3. **T3**：text_cleaner 新增 `clean_inline_watermark`/`light_clean`：
   - 零风险类：`汉8-汉/汉8-行尾/行首-0+数字` 直接删（不碰「沪指-0.11%」负数）；
   - 上下文限定类：`汉(20|26|08|28)汉` 仅当 前字非约数词(约/超/前/共…) 且 后字非量词(亿/万/家/起/日…) 才删，
     「约20亿美元/前20的/超20起/8月20日」全部保留（12 组用例入测）；
   - normalize 阶段对 title/snippet/content 做 `light_clean`，噪声不再进入聚类与 LLM 上下文；
   - `ANALYST_CACHE_VERSION` v3→v4，含噪声旧分析缓存自动失效。

### 9b · P1
4. **T4**：history 目录日期按 23:59:59 与窗口比较，days=3 足天覆盖（入测）。
5. **T5**：backfill 查询词改 `build_backfill_queries()` 按运行日期动态生成年份；`time_window_days` 接线。
6. **T6**：`_extract_published_date` 增加无年份月日信号（「M/D」「M月D日」，按报告年推断、
   `valid()` 淘汰越界年）。实测「美股 8/28 收盘」获得 published_at → 被 24h 时效过滤丢弃。
7. **T7**：main.py collect 接 yaml filtering/time_window（与 run_daily 同口径）；文件名 ts 改报告时区。
8. **T9**：add_us_stocks `articles_to_items` 先过滤短摘要再取前 N（入测：前 3 篇过短仍补足 6 条）。
9. **T10**：事实核查/溯源后的分析回写事件指纹缓存（`analyst.use_cache` 门控），重跑不再重复 LLM 复核。

### 9c · P2 收敛
10. **T11**：exceptions `handle_api_error` 401/400 分支改传 `details=` 字典（原 kwargs 直接 TypeError，入测）；
    `ErrorHandler.log_error` 用 `str(error)`。
11. **T12**：extract 缓存收敛到 SmartCache：`cache_extract_text(url, text, ttl_hours=)` 可配 TTL；
    run_daily 删除自造 sha1 文件缓存。
12. **T13**：LLMClient 模型名优先级 显式参数 > `LLM_MODEL` 环境变量 > yaml `llm.model`（死配置激活）。
13. **T14**：run_daily `init_config(watch=False)`，批处理不再拉起 1s 轮询线程。
14. **T15**：`get_logger` 单例缓存（不再每次 clear+新建 FileHandler），log_dir 接 `config.log_dir`。
15. **T16**：`RunReport.stage()` 返回耗时；run_daily 各阶段 `perf_monitor.record_timing`，
    runlog 新增 `performance` 键。实测：collect 10.3s / extract 36.5s / analyze 892.5s 落盘。
16. **T17**：collector 删除 ThreadPoolExecutor/as_completed 死代码与未用 executor。
17. **T18**：`git rm scripts/pdf_to_report.py.bak`。

### 9d · P3 + 测试
18. **T19**：事实核查 LLM 复核改 `run_in_executor` + 信号量并发，不再串行阻塞事件循环。
19. **T20**：`analyze_event_async` 改 `asyncio.get_running_loop()`。
20. **T21**：`_add_time_qualifier` 改报告时区 `report_now()`。
21. **T22**：聚类阈值接 yaml `defaults.cluster_title_threshold`（默认 0.6）。
22. **T23**：模板目录/栏目序号改可见计数器（Jinja namespace），空栏目不跳号（PDF 目检「一/二」连续）。
23. **T24**：`_write_summary` 注释与代码对齐（70 分）。
24. **T25**：SmartCache 启动时主动 `_cleanup_file_cache()`。
25. **T26**：`collect_articles` 注解改 tuple；清理未用导入；**另修复基线潜伏缺陷**——
    run_daily 从未导入 `load_strategy/generate_queries`（被 T1 崩溃掩盖的第二个入口即崩点）。
26. **T27**：新增 `tests/test_round3_regressions.py`（12 用例）：
    - 入口冒烟：无模式参数正常退出；`--test --no-agent` 主路径（stub 采集器）跑通 collect→cluster；
    - Tavily 分支冒烟（stub 双引擎，拦截 tasks 回归）；
    - 内联清洗 12 组真实噪声/合法数字用例；T4 足天窗口；T6 M/D 日期；T9 选条；T11 kwargs。

### 真实环境验证（2026-08-31，`run_daily.py --test`）

- Step1 双引擎：AnySearch 7/12 + Tavily 70 篇 → 95 篇（时效丢弃 27 旧闻）；
- Step2：82 事件，均衡选 6；Step3：6 事件分析 + 事实核查；日报 JSON 落盘；
- 产物标题/正文内联水印噪声 **0 命中**（08-30 旧报有 7 处）；
- PDF：python3.12 解释器缺 playwright（环境问题，非代码回归），用含 playwright 的
  homebrew `python3` 补渲染成功（484.5 KB），目录序号正常。
- **运维建议（写入发布检查单）**：固定生产解释器或为 3.12 安装 playwright；
  每批提交后跑 `--test` 真实链路 + `pytest` 双验证。
